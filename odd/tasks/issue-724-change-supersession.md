# Issue 724 — Change-Level SDD Supersession

## Objective

On authorized task replanning, seal the old protected stream under its original manifest, then create one new change with a fresh stream under the new manifest. Preserve every historical head, batch and receipt without transferring task credit. GitHub contract: https://github.com/Thrasno/jarvis-ai-devs/issues/724 (`status:approved`).

## Problem and why

The task manifest is stream identity. A mismatch currently blocks progress but offers no supported low-friction recovery. Changing the manifest in place, adopting an existing successor, or inferring that zero credited tasks means zero immutable evidence would compromise authority. The ordered two-change protocol must tolerate crashes without pretending to be a distributed transaction.

## Scope and constraints

- One terminal `superseded` seal in the predecessor, with its old manifest, no new evidence/coverage and signed frozen successor intent. New successor has no head, starts at 1/1 and signs `supersedes`.
- The single-change shared seal-shape predicate lives in `hivederive/applyprogress`; store adapters branch only on that exact form, validate the published old head and referenced immutable evidence, and do not reconstruct old tasks. Ordinary advances remain strict.
- Cross-change link integrity, derived `superseded_pending_successor`, routing and diagnostics belong to SDD status/continue, not the single-change model.
- No transferred completion credit. Zero credited tasks permits automatic handoff after all safety checks; nonzero requires explicit consent before sealing; a decline changes neither change.
- New successor name must be unoccupied. Intent frozen in seal includes target manifest, actor, reason, timestamp and stable operation/request identity so retries reconstruct exact input.
- Existing per-change OpenSpec/Hive/hybrid binding, CAS and receipts own publication. No cross-change transaction, existing-change adoption, Git identity promotion or `none`-mode protected stream.
- Versioned snapshot schema preserves historical v2 bytes/digests and historical continuation decode. Generated user-machine configuration is never edited; source templates only.

## Delivery and checks

- Branch: `feat/issue-724-change-supersession` from `master`/`public/master` at `2ca854c84040c593e9232beac72305c2a6a23d27`.
- Forecast: 1,740–3,310 authored diff lines (730–1,350 production Go, 840–1,560 tests, 170–400 docs), approximately 17–27 files. Addition + deletion both count; the per-task 400-line target is advisory, not a reason to omit tests or artificially split a behavior.
- Delivery strategy: `ask-on-risk`, resolved by the maintainer to `feature-branch-chain`. Plan one tracker plus independently reviewable child slices; push/PR/merge remain separate user decisions. Never create a single oversized PR by default.
- TDD: **on**, source: `AGENTS.md` strict TDD for SDD implementation and `openspec/config.yaml` `strict_tdd: true`; exact focused runner: `go test -count=1 ./applyprogress` from `hivederive` for model, `go test -count=1 ./internal/sddprogress ./internal/sddstatus ./cmd/jarvis` from `jarvis-cli` for integration, and `go test -count=1 ./internal/db` from `hive-daemon` for daemon. Record observed RED, GREEN and refactor checks per task; no builds without explicit request. Final applicable checks: `go test -count=1 ./...` and `go vet ./...` separately in each affected Go module.
- All tasks route through a bounded `gentle-ai-worker` for 2+ non-trivial files. Workers may edit only the named surfaces, run authorized focused tests, and never commit/push/approve. Parent keeps task file, memory mirror, review, and work-unit commits.

## Actionable checklist

- [x] **ODD-724-01 — Versioned seal and successor model.** Shared snapshot schema compatibility, strict old-v2 decoder/digest preservation, terminal `superseded`, signed seal intent and `supersedes`, shared seal-shape predicate, `ValidateSuccessor` transition and negative cases. Focused `hivederive` package RED/GREEN and vet; include regressions for legacy explicit-zero and continuation. Candidate work-unit commit: `feat(sdd): model change supersession in protected progress`.
- [x] **ODD-724-02 — OpenSpec exact-seal publication.** Branch manifest/candidate/current validation only on shared seal shape, validating signed previous head and immutable referenced batches while bypassing current tasks solely for seal. Preserve receipt replay and ordinary strictness; cover changed tasks, continuation-bound head, concurrent writer, corrupt evidence and retries. Focused CLI `internal/sddprogress` RED/GREEN. Commit with tests.
- [x] **ODD-724-03A — Hive daemon exact-seal publication.** Branch daemon authoritative-task validation only on shared seal predicate using the signed current head and immutable referenced batches inside the existing transaction. Preserve CAS/receipt identity and ordinary strictness; test changed Hive tasks, existing batch corruption, competing writer, exact replay and terminal read. Focused daemon DB RED/GREEN, commit with tests.
- [x] **ODD-724-03B — CLI and hybrid seam seal retry.** Keep normal CLI checkpoint strict; exercise the existing advance route and exact hybrid partial-publication recovery with two independent durable backends. Test changed tasks, divergent sides, interrupted sides, exact replay and invalid evidence. This is seam-level evidence, not a real Hive end-to-end test. Commit with tests.
- [x] **ODD-724-03C — Real Hive HTTP seal contract.** Exercise actual daemon DB/API-backed advances after revising real authoritative tasks, preserving old manifest/evidence, exact retry and request/CAS conflicts. Combined daemon+OpenSpec Hybrid E2E is separately tracked as blocked by the Go module/internal-package boundary; this test does not claim to cover it.
- [ ] **ODD-724-06 — Combined live-Hive Hybrid acceptance.** Pending explicit choice of cross-module integration mechanism (and permission if it builds processes or changes module dependencies); only then test a real daemon alongside OpenSpec under partial publication and exact recovery. Until then report this check as skipped, not passed.
- [x] **ODD-724-04A — Cross-change genesis validator.** Validate a distinct-change fresh 1/1 successor against an authenticated predecessor seal, exact frozen target project/name/manifest and signed pointer, with no inherited credit/evidence. Preserve same-change `ValidateSuccessor` strictness. RED/GREEN in shared model, commit with tests.
- [x] **ODD-724-04B1A — OpenSpec successor claim from signed seal.** The signed predecessor seal is the sole durable intent; write no second source-side reservation. Acquire deterministic locks, verify canonical sibling/name and frozen new-task manifest, create final target with exclusive `os.Mkdir`, write seal digest first with `O_EXCL`, then other target artifacts with `O_EXCL`. On retry, resume only when target carries the exact seal digest; an existing target without it (including a crash-created empty directory) fails closed with a clear diagnostic. No final completion marker before genesis is durable. Commit with tests.
- [x] **ODD-724-04B1B — OpenSpec successor genesis publication.** Read stored seal and target claimed by its digest; derive 1/1 genesis, write remaining artifacts with exclusive creation, publish through receipt/CAS, and write final `O_EXCL` completion marker only after durable genesis. Status treats absence of marker as pending; exact retry with marker validates completion, while digest-only target resumes. Never adopt arbitrary artifacts, bypass ordinary advance or claim cross-change transaction. Commit with tests.
- [x] **ODD-724-04B2 — Hive successor authority.** Provide server-side guarded source-seal check and atomic target genesis/receipt under existing DB transaction, rejecting an occupied target and mismatched frozen intent. Commit with tests.
- [x] **ODD-724-04B3A1 — Hive successor governance and HTTP.** Expose guarded DB operation via governance and POST of predecessor with project only. Strict JSON, no caller-supplied successor authority, real SQLite HTTP success/replay/blocked tests. Commit with tests.
- [x] **ODD-724-04B3A2 — CLI Hive successor client.** Add typed request/response API for predecessor publication, preserving exact replay/error envelope. Commit with tests.
- [x] **ODD-724-04B3B1 — Hybrid successor orchestration.** Verify exact authenticated predecessor seal on both sides before publishing OpenSpec first, then Hive; verify both 1/1 genesis results and exact retry after partial publication. No adoption of foreign target. Commit with tests.
- [x] **ODD-724-04B3B2A — Hive adapter for Hybrid successor.** Implement the already-reviewed client call on `hiveProgressAdvancer` with signed-pair response validation; no public command or new binding adoption. Commit with tests.
- [ ] **ODD-724-04B3B2B — CLI successor route after consent gate.** Resolve existing predecessor binding only and route to Hybrid after the 05 consent decision; never expose a public publication bypass or adopt a successor binding. Commit with tests.
- [x] **ODD-724-04C1 — Read v3 progress safely in status.** Recognize authenticated sealed predecessors and v3 successors in Hive/OpenSpec/Hybrid without legacy fallback, preserve historical v2 behavior, block apply/verify/archive on the sealed predecessor, validate immutable evidence and store parity. This unit does not assert that a successor was published. Commit with tests.
- [x] **ODD-724-04C2 — Cross-change status and continue routing.** Verify stored predecessor seal and committed successor pointer/manifest, distinguish absent or unmarked target as derived `superseded_pending_successor`, fail closed on mismatch, and route status/continue only after a validated pair. Test interrupted publication and same-project identity. Commit with tests.
- [ ] **ODD-724-05 — Consent, archive and guidance.** Zero/nonzero credited task UX, decline without mutation, old archive rejection, source-of-truth embedded guidance and user docs, command/status integration regressions across stores and historical snapshots. Complete relevant module test/vet checks, record failures/skips, work-unit commit and review boundaries; do not publish PRs without user authorization.

## Acceptance criteria

The bilingual approved issue body is normative. In particular: old immutable evidence survives, `complete` or previously `superseded` heads cannot be sealed, no old tasks are reconstructed, all three bound stores accept only the exact seal, no successor is adopted, a crash leaves visible derived pending status recoverable by identical retry, credit never transfers, archive cannot treat superseded as complete, old v2 digests remain valid, and promotion of Git project identity is out of scope.

## Progress

- [x] Read-only code/issue research and line forecast completed; issue approved by maintainer.
- [x] Feature branch created from clean `master`; delivery chain strategy chosen explicitly.
- [x] ODD-724-01 complete: snapshot v3 model, exact seal predicate, signed intent and pointer, historical v2 compatibility. Delegated writer observed RED then GREEN; independent verifier found and corrected a generation-zero historical-root seal defect; subsequent uncached focused tests, vet and diff checks passed. Work-unit commit `714b9ee4008ee02e75b54343ff279829f9eb6795` (408 authored diff lines, including this plan's initial version). Native review lineage `review-becc0cb1d29ffd85` approved and acknowledged with burned authority.
- [x] ODD-724-02 complete: OpenSpec exact seal after task edit, prior signed head integrity, historical receiptless-head preservation, exact receipt retry and fail-closed staged recovery. Independent verifier initially found two receiptless authority gaps; both corrected, followed by PASS. Work-unit commit `3483eea8d44da237aa3140266026cfcbd0522f7c` (225 authored diff lines). Native review lineage `review-33b18d39b59a1fe2` approved and acknowledged with burned authority.
- [x] ODD-724-03A complete: Hive daemon accepts only an exact shared seal after authoritative tasks change, preserving ordinary task checks, CAS and receipts. TDD RED/GREEN, independent focused tests/vet PASS, commit `ee7af762a59b82b11b270adce44ac379bf996abd` (95 authored lines). Native review `review-82e3dcc864786c90` approved and acknowledged; nonblocking informational warning `R3-001` at `hive-daemon/internal/db/apply_progress_test.go:73-81` noted for later follow-up, not an open correction.
- [x] ODD-724-03B complete at hybrid seam: two independent OpenSpec stores simulate both publication orders, interrupted writes, exact retry, divergent heads and corrupt evidence. No production code change was justified; tests passed immediately (no RED observed). Independent verifier classified real-Hive E2E as PARTIAL, so it is tracked as separate 03C rather than claimed. Commit `43328b5f670834fa360473ecb06f21162477c667` (117 authored lines), native review `review-23835c304129be0a` approved and acknowledged.
- [x] ODD-724-03C: real SQLite/governance HTTP seal with old and revised task manifests, replay, conflicting receipt and stale CAS, old immutable batches and current head. Independent verifier PASS, commit `103bb6c918f32e3a90690f2d49a1ed9db405bb69` (108 authored diff lines), native review `review-b8219c2e07bfb238` approved/acknowledged. Combined real-daemon Hybrid E2E is not proven, separately blocked in 06; no new product scope.
- [x] ODD-724-04A pair-shape validator `ValidateSuccessorGenesisPair` binds signed predecessor/successor coordinates, manifests, pointer/intent, 1/1 genesis and empty credit/evidence. Its explicit caller precondition demands an authoritative stored sealed predecessor; pair shape alone is not authority. Independent verifier first FAIL on ambiguous authority claim, then PASS after API/doc correction. RED/GREEN focused model checks observed by writer; same-change `ValidateSuccessor` untouched.
- [x] ODD-724-04A commit `d98a7f7b3e8f027338cd545c9d706df40fc1afdd` (111 authored diff lines), native review `review-c5690b27dd3a3e9d` approved/acknowledged.
- [x] ODD-724-04B1A: the maintainer's portable seal-only claim is implemented in `reserveSuccessor`: canonical stored seal, deterministic sibling locks, revised source tasks bound to frozen manifest, exclusive final `Mkdir`, first raw digest file `O_EXCL`, then tasks `O_EXCL`, exact digest-only retry with file/directory resync. Empty target after crash before digest fails closed; arbitrary headless target never adopted. First unsafe candidate was restored with explicit approval; second was replaced, never committed/reviewed while unsafe. Independent verifier initially FAIL on durability/lock path then PASS after correction. Commit `3ad0c7f0fe21b880fed12b0d44034f214a1d963a` (374 authored lines), native review `review-1b8aa3151512c435` approved/acknowledged. Read-only challenge of a historical receiptless predecessor was refuted: `Advance` seeds predecessor authority before seal publication and the existing receiptless regression checks `Current`.
- [x] ODD-724-04B1B: authenticated OpenSpec genesis 1/1 from stored predecessor seal and digest-owned target, public Advance/AdvanceLegacy bypass blocked, CAS/receipt publication, and final completion marker after durable head. Exact retry/foreign receipt tests PASS; command-running independent verifier PASS, separate verifier PARTIAL due authorization timeout. Commit `a24883686564d05bc2ca38c911731964d5f3680f` (269 authored lines), native review `review-c72aa8a3f08ee09a` approved/acknowledged with nonblocking informational R3-001 at `openspec.go:198-200`.
- [x] ODD-724-04B2: dedicated Hive successor DB operation after seal, not an effect inside seal Advance. Canonical signed terminal is sole authority; latest replanned source tasks must match frozen target manifest. Reject target history including deleted/Unicode/legacy names, insert target tasks and 1/1 genesis/head/receipt in one transaction. Direct forged pointer blocked; exact replay stays read-only even with blocked project and stale local head, while new work reconciles head in the same transaction. Independent verifier PASS; delegated writer did not return RED transcript, so TDD order remains unconfirmed. Commit `f062474eab1f14c2c4478801299adea4657426cb` (398 authored lines), native review `review-89eca5005fa1ed93` approved/acknowledged. 04B3A transport next.

- [x] ODD-724-04B3A1: guarded daemon governance and POST `/sdd/changes/{change}/apply-progress/publish-successor` with project-only strict JSON; real SQLite-backed HTTP test covers committed genesis, blocked-project exact replay 200 and new work 423; existing advance blocked error mapping is aligned. Independent verifier PASS; RED/GREEN transcript not captured by parent. Commit `706b995edae29eb5189300906452c8df4750e42c` (224 authored diff lines), native review `review-39d4ddc37b93baec` approved/acknowledged with informational R3-001 at `server.go:536-537`.

- [x] ODD-724-04B3A2: typed Hive client POST sends only project to predecessor path and preserves committed result/receipt or typed 423/422 error. Independent focused verifier PASS; cross-process client+daemon integration not claimed. Commit `727b5ab55db08b48ca9230984dbb2840e0d19861` (75 authored diff lines), native review `review-723aae7ebbbb5a79` approved/acknowledged.

- [x] ODD-724-04B3B1: Hybrid validates matching authenticated predecessor seals on both stores, publishes OpenSpec first, verifies its result against stored genesis before mutating Hive, then validates Hive returned genesis against signed pair. Exact replay after interrupted second publication, forged result and occupied target tests. The real Hive adapter intentionally belongs to 04B3B2; this unit alone is not callable from CLI. Independent verifier PASS. Commit `7612da146d2f65c24fd4c595494494dc4e0498d9` (285 authored diff lines), native review `review-19c43c493e7a0a25` approved/acknowledged.

- [x] ODD-724-04B3B2A: real Hive client adapter now fulfills Hybrid successor interface, derives remote request only from the authenticated predecessor seal, propagates typed HTTP failures, and rejects noncommitted, inconsistent or forged genesis responses. Independent focused verifier PASS using httptest client; no public CLI publication command/consent bypass. Full Hybrid+daemon E2E remains blocked as 06. Commit `784e4c80db9e837eea986b9cb4c9955956855ba2` (106 authored diff lines), native review `review-f873faa9fcb6af39` approved/acknowledged.

- [x] ODD-724-04C1: guarded v3 Hive/OpenSpec/Hybrid reads now recognize authenticated superseded predecessor separately from ordinary partial progress; seal evidence coverage and project/change identity are checked, JSON-like legacy fallback blocks, Hybrid requires matching sealed heads, and apply/verify/archive stay blocked on predecessor. Existing archive fixture identities were made explicit rather than weakening guard. Independent verifier PASS on focused tests/vet/diff; RED transcript unavailable from delegated writer. This unit deliberately does not validate cross-change pointer or marker; 04C2 pending. Committed as `c47b290c09714996f1012ff5c8815a444af564bf`; native review `review-7265a2b0fe087a9b` approved and acknowledged. Informational R2-001/R3-001 are non-blocking follow-ups.

- [x] ODD-724-04C2: authenticated stored seal and successor head pair, signed intent and marker/receipt gate, claimed interrupted OpenSpec genesis as derived Pending only for exact content-bound staging/receipt, Hive/Hybrid parity and bounded A→B→C routing in human/JSON status and continue. Real `BeforeRename` RED/GREEN and exact retry tested; foreign staged bytes and expected-name receipt payload rejected. Independent verifier PARTIAL under its unexpected-mutation contract: external gofmt was reported during audit, before its first digest; all authorized checks passed on the post-mutation seven-path diff (`9cf381d4144bb3f376c14ecee42a7b7e2629064169de6ffa208916600b5dc212`), matching the parent's snapshot, with no concrete functional failure found. Pre-mutation byte identity was not established; native review `review-d34f3df052e8321e` approved and acknowledged for target `sha256:9853bd074b1501257dd779fca4ae92112dd683fefe4941576aeab9a986287875`. Informational R2-001/R2-002 remain later follow-ups, not corrections. No combined live Hive+OpenSpec E2E or atomic cross-change read claim.

- [x] ODD-724-05 preparatory gate/archive/guidance slice: local `requireSupersessionConsent` validates a signed partial predecessor and distinct successor ID; zero signed credit skips the prompt, nonzero credit requires typing the exact successor name. It has no public caller or mutation authority. Archive rejects a real OpenSpec superseded predecessor despite an injected ready status and preserves its head/receipt/topology; Hive/OpenSpec/Hybrid validator tests reject superseded, existing complete-v2 archive and locked verify-report revalidation regressions remain. Embedded source guidance and user guide distinguish this gate from the still-unimplemented public command. Independent auditor PARTIAL for the full 05 outcome because stored-head/manifest/occupancy preflight and lock/CAS revalidation are not yet wired; focused tests/vet/diff passed on a stable six-path candidate (`4483b8a915fce09b53cb7de1c899b3b01adb5a0cd66818c2d97540d1b37f3a6e`). Native review `review-68d4994e749877b8` approved/acknowledged on final code/doc candidate, with informational R2-001/R2-002/R3-001/R3-002, not correction transitions. The full 05 task remains open until 04B3B2B integrates and proves consent before sealing.

## Verification evidence and work-unit commits

WU-01: `cd hivederive && go test -count=1 ./applyprogress` PASS; `cd hivederive && go vet ./applyprogress` PASS; `git diff --check` PASS; independent read-only verifier PASS after correcting its first FAIL. Native ASSESS over committed range returned `unassessable` (`schema-incompatible`); committed-range inspect/start/reviewer completed and acknowledgement burned as `review-becc0cb1d29ffd85`. Work-unit commit: `714b9ee4008ee02e75b54343ff279829f9eb6795` (368 additions, 40 deletions).
WU-02: `cd jarvis-cli && go test -count=1 ./internal/sddprogress` PASS; `cd jarvis-cli && go vet ./internal/sddprogress` PASS; `git diff --check` PASS; independent read-only verifier PASS after two receiptless-head corrections. Native ASSESS over committed range returned `unassessable` (`schema-incompatible`); committed-range review `review-33b18d39b59a1fe2` approved and acknowledged. Commit `3483eea8d44da237aa3140266026cfcbd0522f7c` (220 additions, 5 deletions).
WU-03A: `cd hive-daemon && go test -count=1 ./internal/db` PASS; `cd hive-daemon && go vet ./internal/db` PASS; `git diff --check` PASS; independent verifier PASS. Native ASSESS `unassessable` (`schema-incompatible`), committed-range review `review-82e3dcc864786c90` approved/acknowledged with advisory warning R3-001. Commit `ee7af762a59b82b11b270adce44ac379bf996abd` (88 additions, 7 deletions).
WU-03B: `cd jarvis-cli && go test -count=1 ./internal/sddprogress ./cmd/jarvis` PASS; `cd jarvis-cli && go vet ./internal/sddprogress ./cmd/jarvis` PASS; `git diff --check` PASS; no RED because the new behavior test passed against existing hybrid production code. Independent verifier PARTIAL specifically for real Hive DB/API evidence, preserved as 03C. Native ASSESS `unassessable` (`schema-incompatible`), review `review-23835c304129be0a` approved/acknowledged. Commit `43328b5f670834fa360473ecb06f21162477c667` (113 additions, 4 deletions).
WU-03C: `cd hive-daemon && go test -count=1 ./internal/httpapi` PASS; `cd hive-daemon && go vet ./internal/httpapi` PASS; `git diff --check` PASS; independent verifier PASS for real daemon HTTP contract. New test passed existing code (no RED); no combined Hybrid E2E. Native review `review-b8219c2e07bfb238` approved/acknowledged. Commit `103bb6c918f32e3a90690f2d49a1ed9db405bb69` (104 additions, 4 deletions). Broader module tests and remaining units pending.
WU-04A: `cd hivederive && go test -count=1 ./applyprogress` PASS; `cd hivederive && go vet ./applyprogress` PASS; `git diff --check` PASS; independent verifier PASS after naming/documenting pair-shape vs authoritative-store boundary. The 04B caller must load canonical stored sealed head; a self-digested forged pair alone must never be accepted as authority. Commit `d98a7f7b3e8f027338cd545c9d706df40fc1afdd` (106 additions, 5 deletions); native review `review-c5690b27dd3a3e9d` approved/acknowledged.

WU-04B1A: `cd jarvis-cli && go test -count=1 ./internal/sddprogress` PASS; `cd jarvis-cli && go vet ./internal/sddprogress` PASS; `git diff --check` PASS; independent verifier PASS after fixing file resync and canonical lock ordering. This is claim-helper evidence only, not completed genesis. Commit `3ad0c7f0fe21b880fed12b0d44034f214a1d963a` (365 additions, 9 deletions), native review `review-1b8aa3151512c435` approved/acknowledged.

WU-04B1B: `cd jarvis-cli && go test -count=1 ./internal/sddprogress` PASS; `cd jarvis-cli && go vet ./internal/sddprogress` PASS; `git diff --check` PASS on current candidate. One independent verifier was PARTIAL because its command authorization timed out; a separate read-only verifier ran the exact commands and found no concrete blocker. `AdvanceLegacy` now shares the public genesis guard; orphan evidence and foreign receipts fail closed. Tests cover exact retry of head without marker and finished marker, plus invalid receipt. Commit `a24883686564d05bc2ca38c911731964d5f3680f` (259 additions, 10 deletions), native review `review-c72aa8a3f08ee09a` approved/acknowledged; informational R3-001 at `jarvis-cli/internal/sddprogress/openspec.go:198-200` is nonblocking follow-up.

WU-04B2: `cd hive-daemon && go test -count=1 ./internal/db` PASS; `cd hive-daemon && go vet ./internal/db` PASS; `git diff --check` PASS, independent read-only verifier PASS after correcting direct-genesis bypass, historical replay, Unicode occupancy and replay-mutation defects. No local seal receipt is required because signed terminal lineage is authority and synced receiptless seals remain eligible. Commit `f062474eab1f14c2c4478801299adea4657426cb` (394 additions, 4 deletions), native review `review-89eca5005fa1ed93` approved/acknowledged.

WU-04B3A1: `cd hive-daemon && go test -count=1 ./internal/governance ./internal/httpapi` PASS; `cd hive-daemon && go vet ./internal/governance ./internal/httpapi` PASS; `git diff --check` PASS; independent verifier PASS after adding real SQLite transport tests. Commit `706b995edae29eb5189300906452c8df4750e42c` (220 additions, 4 deletions), native review `review-39d4ddc37b93baec` approved/acknowledged with nonblocking R3-001.

WU-04B3A2: `cd jarvis-cli && go test -count=1 ./internal/hiveclient` PASS; `cd jarvis-cli && go vet ./internal/hiveclient` PASS; `git diff --check` PASS; independent verifier PASS. No integrated daemon+client test. Commit `727b5ab55db08b48ca9230984dbb2840e0d19861` (71 additions, 4 deletions), native review `review-723aae7ebbbb5a79` approved/acknowledged.

WU-04B3B1: `cd jarvis-cli && go test -count=1 ./internal/sddprogress` PASS; `cd jarvis-cli && go vet ./internal/sddprogress` PASS; `git diff --check` PASS; independent verifier PASS after adding OpenSpec returned-result validation. Real adapter pending 04B3B2. Commit `7612da146d2f65c24fd4c595494494dc4e0498d9` (277 additions, 8 deletions), native review `review-19c43c493e7a0a25` approved/acknowledged.

WU-04B3B2A: `cd jarvis-cli && go test -count=1 ./cmd/jarvis ./internal/sddprogress` PASS; `cd jarvis-cli && go vet ./cmd/jarvis ./internal/sddprogress` PASS; `git diff --check` PASS; independent verifier PASS. No combined live Hive E2E. Commit `784e4c80db9e837eea986b9cb4c9955956855ba2` (102 additions, 4 deletions), native review `review-f873faa9fcb6af39` approved/acknowledged.

WU-04C1: `cd jarvis-cli && go test -count=1 ./internal/sddstatus ./cmd/jarvis` PASS; `cd jarvis-cli && go vet ./internal/sddstatus ./cmd/jarvis` PASS; `git diff --check` PASS; independent read-only verifier PASS after correcting sealed routing, JSON fallback, immutable coverage, cross-store parity and requested identity. No dedicated historical v2 status digest fixture in this scope; 04C2 pointer/marker unverified. Commit `c47b290c09714996f1012ff5c8815a444af564bf`; native review `review-7265a2b0fe087a9b` approved and acknowledged. R2-001/R3-001 are informational, not a correction transition.

WU-04C2: `cd hivederive && go test -count=1 ./applyprogress` PASS; `cd hivederive && go vet ./applyprogress` PASS; `cd jarvis-cli && go test -count=1 ./internal/sddstatus ./cmd/jarvis` PASS; `cd jarvis-cli && go vet ./internal/sddstatus ./cmd/jarvis` PASS; `git diff --check` PASS. Independent read-only audit verdict PARTIAL under the unexpected-mutation contract: an external gofmt notice arrived during audit before the first verifier digest. All five authorized checks passed on the post-mutation tree; no concrete functional blocker was found, and the seven-path diff SHA256 `9cf381d4144bb3f376c14ecee42a7b7e2629064169de6ffa208916600b5dc212` matched the parent's snapshot. Pre-mutation bytes were not captured, so stability across the notice is unproven. A separate independent rerun on committed code at `08897e59` returned PASS for the same focused tests/vet/diff: clean worktree and matching seven code/test blobs, with `git ls-files -s` digest `09454fbc197fc57a67a5933013abd98022a631999d18f2feefd3b8273205bfbc` and identical working-file SHA256 before/after. This new PASS does not revise the earlier PARTIAL or claim live Hybrid E2E. Native uncommitted-target review `review-d34f3df052e8321e` approved/acknowledged; informational R2-001/R2-002 do not reopen it. No live combined Hybrid E2E; cross-change reads can become stale after validation.

WU-05-preflight: `cd jarvis-cli && go test -count=1 ./cmd/jarvis ./internal/sddstatus ./internal/sddprogress ./internal/sddruntime` PASS; `cd jarvis-cli && go vet ./cmd/jarvis ./internal/sddstatus ./internal/sddprogress ./internal/sddruntime` PASS; `git diff --check` PASS, six-path diff SHA256 `4483b8a915fce09b53cb7de1c899b3b01adb5a0cd66818c2d97540d1b37f3a6e` stable before/after independent audit. Gate writer observed RED undefined helper then GREEN; archive regressions passed existing production guard without a production RED (fixture-only initial failure is not production RED). Independent verdict PARTIAL for full 05 because no public caller yet. Native review `review-68d4994e749877b8` approved/acknowledged with informational findings; 05 and 04B3B2B integration remains open.

## Next step

04C2 cross-change authority/routing passed on one behavior-level review unit (761 authored diff lines in seven code/test paths); 04C is complete. The maintainer chose two behavior-level review units rather than splitting 04C2 mechanically. Map 05 consent gate before public route 04B3B2B. 04B3B2A adapter committed and reviewed as `784e4c80`. No public publication bypass from canonical predecessor seal. 04B3A2 client committed/reviewed as `727b5ab5`. 04B3A1 committed/reviewed as `706b995e`. Keep 04B2's signed-terminal authority, read-only receipt replay and new-work reconciliation intact. Preserve the reviewed OpenSpec commits; R3-001 remains informational. Do not describe a digest-only claim as a finished successor; 04C status must report it pending. Keep ODD-724-06 blocked until an explicit cross-module test mechanism is approved; do not describe seam+HTTP evidence as combined Hybrid E2E. Reconcile local task file, full Engram mirror and visible todo before next source write.
