# Issue 491: Interactive onboarding delivery checklist

Objective: Provide a standalone, accessible Nexus-styled checklist in `docs/` for tracking the documentation and observed evidence required by issue #491, including explicit tasks owned by Andrés.

Problem/evidence: Issue #491 is a documentation/evidence tracker, not product code. Its seven observed journeys require real human/platform evidence; the page must not imply that checking a box proves an acceptance criterion. The current docs include a standalone Nexus-themed HTML audit at `docs/advisory-review-lite-audit.html`.

Scope: Add one standalone HTML document at `docs/issue-491-onboarding-checklist.html`. Local-only checkbox persistence via a versioned first-party cookie; browser-local fallback for `file://` or cookie restrictions; no analytics, external assets, uploads, or changes to generated user configuration. Include current documentation links, evidence requirements, handoff tasks and privacy constraints. The HTML is a planning aid, not the completed onboarding guide. Content follows this issue's Spanish context.

Acceptance: Every actionable deliverable and each of seven owner-provided evidence journeys are represented with accessible, clickable checkboxes and an accurate progress count. Check state survives reopening where browser storage is available; storage limitation is explicit. A visible reset control does not delete evidence files. No unchecked task is represented as done by default. The page remains usable without JavaScript as a static checklist.

Testing: Strict TDD is recorded in `openspec/config.yaml`; no Go code is changed. A transient, non-repository Node VM harness reproduced RED then GREEN on cookie fallback and exercised reset; `vm.Script` checked syntax. Parent checked links and unique IDs; untracked-file whitespace was checked with `git diff --no-index --check`. The attempted process-substitution `node --check` command failed under Node 26 and was superseded by `vm.Script`. Browser E2E was not run; do not claim browser-tested persistence.

Delivery: ask-on-risk; forecast 250–400 authored changed lines for the HTML plus this task document. Single-file implementation route is delegated because preparation and design require broad repository context. No commit/push/PR unless separately requested, per the orchestrator safety rule.

## Tasks

- [x] T1 (delegated writer): Created `docs/issue-491-onboarding-checklist.html` with 21 tasks, seven owner evidence cases, accessible labels/progress and Nexus visual treatment. Node VM behavior RED reproduced stale local fallback; GREEN verified cookie/local mirroring, reset and session-only degradation. Inline script syntax passed. No browser E2E or commit.
- [x] T2 (delegated verifier): Initial VM verifier checked HTTP cookie, file localStorage, blocked storage and reset; found stale fallback. Writer reproduced RED and fixed. A focused independent post-fix Node VM check passed cookie-denied → local → cookie-enabled → local mirror → cookie-denied/reopen with both marks retained, plus `vm.Script`, 21 unique IDs, 10/10 relative targets. `git diff --no-index --check /dev/null docs/issue-491-onboarding-checklist.html` emitted no whitespace errors (exit 1 expected for a new file). Parent spot check passed syntax and 11 links. Real-browser E2E remains untested.
- [x] T3 (parent delivery): User authorized publishing. Child #765 was created/read back with `status:approved` and `type:chore`; parent #491 remains open. Branch `docs/issue-491-onboarding-checklist`, work-unit commit `96ed96e7b32152b106f4343a1c6b3f229292b43f`, draft PR #766 at `https://github.com/Thrasno/jarvis-ai-devs/pull/766` with exactly one `type:chore` label and `Closes #765`. Remote branch SHA confirmed. CI was pending at PR readback; browser E2E still pending.

## Progress and next step

Exploration identified the issue body and Nexus audit visual reference. The issue remains `status:needs-review`; this checklist is a planning aid, not issue approval or a claim that observed evidence exists. Initial writer's requested process-substitution `node --check` failed in Node 26; equivalent `vm.Script` syntax check passed. Independent verifier found stale local fallback; writer reproduced RED and corrected it with GREEN in a transient Node VM harness. Native inspect selected both intended untracked paths; ordinary review consent was declined for this candidate (no lineage or mutation), and fallback assessment remained unassessable for untracked files. Independent targeted post-fix verification passed. Browser E2E and actual seven owner evidence journeys remain pending and must not be represented as completed. The user has now explicitly requested a PR and approved a narrow child issue for this checklist. PR #766 is published as draft for remote inspection; CI was pending on initial readback. Next: inspect the HTML from the office, run browser QA, and supply the seven observed journeys for parent #491. These are separate pending product evidence; do not mark the parent complete.
