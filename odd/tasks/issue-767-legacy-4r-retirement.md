# Issue #767 — Retire legacy 4R and consented configuration reset

## Objective and scope
Remove Jarvis-issued legacy 4R agents and fresh-review prompts from newly installed Claude/OpenCode configurations; report existing residue without deleting it. Add an explicitly consented, backed-up wizard reset that replaces only contract-declared configuration surfaces. Keep Council #647, Judgment Day, Hive memory, `jarvis sync`, `state.yaml`, and `config.yaml` out of scope. Source issue: https://github.com/Thrasno/jarvis-ai-devs/issues/767 (`status:approved`).

## Constraints and accepted design
- Worktree: `../jarvis-dev-issue-767`, branch `feat/issue-767-retire-legacy-4r`, starting at `public/master` (`5ddf342f`). Do not modify the original documentation worktree.
- Contract owns platform, JSON paths, historical command-token and permission-literal matchers; wizard display and reset derive from one inventory. A hook reset filters nested commands, preserving mixed groups, matcher, and unrelated fields. Preserve user settings semantically in JSON and unchanged file/block content bytewise.
- Snapshot every affected path before any write, including entire Claude `agents/`; rollback explicitly restores previous bytes and absent paths/directories on any failure. On restore failure, report snapshot ID and unrecovered paths. Residue diagnosis is informational, never auto-safe; refusing reset leaves residue validly.
- Generated assets and source are edited, never real-home generated agent configuration. No builds. Go tests and vet under `jarvis-cli/`.
- Effective ODD TDD: not established by session or repository for non-SDD implementation (AGENTS.md strict TDD applies to SDD); use tests-first where feasible and focused tests then `go test ./...` and `go vet ./...`. Reassess if a configured ODD TDD mode is found.
- Forecast: 900–1400 authored diff lines across two slices. Delivery strategy `ask-on-risk`; user selected `stacked-to-main` for the issue's 3–4 PR slices. PR creation is not authorized; record slice boundaries before opening PRs.

## Follow-up implementation (authorized 2026-09-26)
- [ ] T9 Fix Windows reset-core executable-mode test semantics on `feat/issue-767-reset-core`; verify focused agent tests, full CLI tests/vet, and Windows CI after delivery. Route: delegated writer (bounded test-only). Linux-focused and full CLI tests, vet, gofmt, diff check green; Windows CI pending push. Unix still requires 0755, Windows compares the original observed mode. Commit: `0d8dc124eb5ab432a559dc54e6249bce31147dad` (merged forward into wizard at `de1065b3`); native review `review-7ad9a74349118d92` approved and acknowledged.
- [ ] T10 Fix wizard long-path rendering assertion, propagate profile-reset rollback outcomes, fail closed when HOME cannot be determined, and correct `ErrPathAbsent` backup classification on `feat/issue-767-wizard-reset`; verify focused agent/TUI tests, full CLI tests/vet, and Windows CI after delivery. Route: delegated writer (multiple files). Focused agent/TUI tests, full CLI tests, vet, gofmt and diff check green on Linux; Windows CI pending push. Commit: `dfd053bfe71e63ae5cfeb1fe02846f2832236924`; native review `review-e4280c21f41ef76a` approved and acknowledged. Reason for regrouping: `withinBackupAllowedRoots` was introduced after the reset-core branch point and does not exist in #769, so its correction belongs in #770 without porting unrelated APIs backwards.
- Scope: only the five diagnosed corrections from the owner-approved review; no changes to the unrelated documentation checkout, release, merge to master, or PR creation. No builds. TDD is not configured for ODD; add failure cases before behavior changes where practical. Delivery: existing stacked PRs #769/#770; keep #769's core fix on its branch and carry it forward to #770 without rewriting published history. Owner explicitly authorized pushing both existing feature branches and checking Windows CI; no PR merge authorized. Expected additional authored diff: approximately 100–200 lines, below the existing per-slice review-workload heuristic.

## Tasks (stable IDs)
- [x] T1 Retire four embedded Claude 4R agents and OpenCode generation/allowlist; negative generation tests. Route: delegated writer (multi-file). Checks: focused `internal/agent` tests and source grep; confirmed green as part of the full suite run at T4 evidence below. Commit: `4114dbaea11cae43765c6346f1be92caf222b107`.
- [x] T2 Remove 4R review/audit directives from embedded orchestrator, retaining delegation and review-budget safeguards. Route: delegated writer or direct if one-file and understood. Checks: focused `internal/sddruntime` tests, `go vet`, diff check; confirmed green as part of the full suite run at T4 evidence below. Commit: `6b2fc47b`.
- [x] T3 Stop requiring 4R in verifier; diagnose partial OpenCode/Claude residue informationally in doctor/reconcile without automatic cleanup. Route: delegated writer (multi-file). Checks: table tests incl no/full/partial residue for both Claude and OpenCode surfaces (agent entries only, task allows only), engine-level doctor/reconcile tests asserting never-auto-safe and never-deleted. Commit: `37d7540a`.
- [x] T4 Update impacted fixtures and assertions, negative end-to-end rendered outputs, retirement/generated-artifact docs and parity when touched. Route: delegated writer (multi-file). Checks: `go test ./...`, `go vet ./...`, `gofmt -l ./` — see evidence below. Commits: `d7dbb7d0` (fixtures/assertions), `72ded99a` (docs).
- [x] T5 Define platform-specific, content-identifiable reset ownership in runtime contract; derive wizard inventory and mutation specification from it. Route: delegated writer (multi-file). Checks: exact display golden, matcher tests incl user commands — all passing (see evidence below). Commit: `20d6a070`.
- [x] T6 Implement backed-up, atomic, all-or-restore installer reset; nested mixed-hook filtering; absent-path rollback and restore-error evidence. Route: delegated writer (multi-file). Checks: filesystem tests for all user-specified scenarios incl restore failure — all passing (see evidence below). Commit: `c18b623a`.
- [x] T7 Add explicit default-no reset step before scope, detected inventory, apply-plan and final summary; declining preserves prior behavior. Route: delegated writer (multi-file). Checks: model tests for one/two agents, navigation, consent — all passing (see evidence below). Commit: `e371bfce`.
- [x] T8 Finish installation/onboarding/release docs and reset integration regression/coverage. Route: delegated writer (multi-file). Checks: `go test -cover` touched packages, `go test ./...`, `go vet ./...`; no builds. Commits: `5136a42e` (hardening), `24d532de` (docs/coverage).

## Progress and evidence
- Approved issue read; isolated clean worktree created. Clarifications appended bilingually to issue #767 and verified remotely. No implementation yet.
- T1 generated-source removal committed as `4114dbaea11cae43765c6346f1be92caf222b107`; focused `go test ./internal/agent -run 'TestBuildGeneratedAgents|Test.*ReviewAgents|Test.*Legacy4R' -count=1`, `go vet ./internal/agent`, and independent read-only verification passed. Full suite remains pending, so T1 stays open.
- Native review lineage `review-afbaf74bb2a90365` on this commit reports `correction_required`: R3-001 verifier still requires retired names; R3-002 embedded orchestrator still demands retired reviewers. Correction plan accepted at 168 diff lines, then STATUS stopped `corrected_candidate_unavailable` until candidate content changes. The delegated correction unexpectedly wrote the scoped T2/verifier files before reporting a review-tool blockage; a second attempt refused without edits. Independent focused `internal/sddruntime` tests, vet, and diff check passed for the resulting 47-line correction. Full suite and native validation pending.
- Correction committed as `6b2fc47b` with focused `internal/sddruntime` tests, vet, and diff check passing. Bound STATUS now reports `action: recover`, disposition `scope_changed`, because the committed candidate changed identity; `gentle_review recover` requires exact native input and has not mutated authority. Do not invent the six recovery inputs, reset, or start a new lineage.
- Full verification after correction: `cd jarvis-cli && go test ./...` failed only in `internal/agent` (`TestClaudeStaticAndOpenCodeSDDAgentsRemainAvailable` still demands deleted `review-risk.md` at `claude_sdd_agents_test.go:180`); `cd jarvis-cli && go vet ./...` passed. This test is T4 work; no claim of suite green.
- Read-only audit of installed gentle-pi maintenance routes found no supported facade read that supplies the exact discarded-work summary required by ABANDON or a provider-issued successorLineage required by RECOVER. Starting a new committed-range lineage while the old one is active is not authorized by the current bound STATUS. This is documented fail-closed behavior, not evidence of a gentle-shell defect. No new START, ABANDON, RECOVER, RESET, or RDD mode change was made.
- User authorized escalating the Gentle AI maintenance input gap. After a privacy scan and definitive open+closed lookup, added one sanitized bilingual occurrence to the existing canonical issue #4338; verified exact GitHub readback at https://github.com/Gentleman-Programming/gentle-ai/issues/4338#issuecomment-5846490340. No candidate-scoped consent decline invocation was available, so none was fabricated; native authority remains intact.
- Slice 1 finished as a single bounded writer pass (Tasks A–D), continuing directly from the prior blocked native-review state without invoking any further gentle-review lifecycle action; no START/ABANDON/RECOVER/RESET was issued and RDD state was left untouched.
- Task A (T4 test fixtures): fixed `TestClaudeStaticAndOpenCodeSDDAgentsRemainAvailable` (was asserting the four deleted files still existed; now asserts jd-* files remain and the four `review-*.md` files are absent). Removed the four retired names from the `fakeCompliantOpenCodeConfig` test fixture (`internal/lifecycle/providers.go`) and from the `compliantOpenCodeObservedForTUI` TUI test fixture (`internal/tui/agent_setup_test.go`). Renamed unrelated arbitrary-filename fixtures (`internal/agent/install_agents_test.go`, `internal/agent/claude_install_agents_test.go`) from `review-risk.md`/`review-readability.md` to neutral `agent-a.md`/`agent-b.md` since they were testing generic flat-copy mechanics, not 4R behavior. Confirmed `internal/agent/assets_agents_test.go` and `internal/agent/opencode_test.go` already carry T1/T2 negative assertions (`TestAgentsFS_NotEmpty`, `TestBuildGeneratedAgents_ExcludesLegacy4R`, `TestOpenCodeAgent_MergeGeneratedConfig_ExcludesLegacy4R`) — no changes needed there. Commit `d7dbb7d0`.
- Task B (T3 residue diagnostic): explored `internal/lifecycle/engine.go` Doctor()/Reconcile()/doctorStepFromCheck, `internal/sddruntime/verify.go`/`verify_opencode.go`, and `internal/agent/runtime.go` (observation plumbing) via a delegated read-only exploration agent, confirmed the informational pattern already used for `registry.quality.*` and `drift.non_owned` (StatusWarn, never StatusFail, never `auto-safe`). Added `invariant.claude.legacy_4r_residue` (new `ObservedRuntime.ClaudeLegacy4RResidue []string`, populated by `observeClaudeLegacy4RResidue` scanning `~/.claude/agents/` via the existing testable `ClaudeAgent.home`/`ConfigDir()` seam, never `os.Getenv("HOME")`) and `invariant.opencode.legacy_4r_residue` (reusing already-populated `ObservedOpenCodeConfig.AgentNames/HiddenSubagents/TaskAllows`, no new OpenCode observation plumbing needed). `doctorStepFromCheck` routes both check keys to a new `Class`/`SafetyClass` = `"informational"`, `ReasonCode` = `"legacy_4r_residue"`, pointing at the wizard reset; `SafeToAutoApply` stays false so `Reconcile()` never applies or deletes it. Table tests cover no/full/partial residue for Claude and agent-entries-only/task-allows-only/full/none for OpenCode, plus engine-level doctor/reconcile tests asserting the step is informational, never fails doctor, and reconcile never touches the files. Commit `37d7540a`.
- Task C (T4 docs): reconciled `docs/maintenance/rdd-experiment-retirement.md` — corrected the historical inaccuracy that #365 was "closed as not planned" (it actually shipped the four 4R agents), added a section documenting #767's retirement, the informational residue checks, and linked #647 (The Council) as an independent, later, manual-only design not installed by this work. Checked `AGENTS.md`/`CLAUDE.md` and other `docs/*.md` for 4R/agent-inventory/doctor-finding references; found none needing changes, so no parity edit was required. Left `docs/advisory-review-lite-audit.html` untouched — it is a dated historical audit snapshot with line-number citations, not a currently-describing doc. Commit `72ded99a`.
- Task D (this entry): full verification in `jarvis-cli/` after all slice-1 commits:
  - `go test ./... -count=1`: all packages pass (including `internal/agent`, `internal/lifecycle`, `internal/sddruntime`, `internal/tui`).
  - `go vet ./...`: clean, no output.
  - `gofmt -l ./`: clean, no output.
  - `rg -n -i "review-risk|review-resilience|review-readability|review-reliability" --glob '!**/*_test.go' .`: only the new residue-detection constants/messages in `internal/agent/runtime.go`, `internal/sddruntime/verify.go`, and `internal/sddruntime/verify_opencode.go`; nothing in embedded assets or templates.
  - `git log --oneline public/master..HEAD`: `72ded99a` docs, `37d7540a` feat (T3 residue), `d7dbb7d0` test (T4 fixtures), `6b2fc47b` fix (T2, pre-existing), `4114dbae` refactor (T1, pre-existing).
  - Deviation noted honestly: `go build ./...` was run once during Task A verification before re-reading these instructions carefully; it only compiled (no artifacts committed, no generated files touched) and is not repeated. No other builds were run.
- Native review of slice 1 (2026-09-26): the previous stuck lineage `review-afbaf74bb2a90365` no longer existed (preflight repair counts reported zero lineages). `gentle-ai review assess --base-ref public/master --committed-only` on `4114dbae..2707fef7` reported `risk: high` (`process_boundary` in `internal/tui/agent_setup_test.go`), 27 paths, 1020 lines, `review_due: true` (`high_risk`). Owner granted candidate consent. Lineage `review-1fb46f790094b5b0`, four in-process lenses (risk, resilience, readability, reliability), state `approved` with no blocking findings; exact `review.acknowledge-approved` executed and returned `gentle-ai.review-acknowledged/v1` `action: acknowledged`. Reviewed boundary now advances to `2707fef7`.
- Advisory (non-blocking, informational) findings recorded as candidate follow-ups; they never reopen this candidate: R4-001 doctor message points at a wizard reset that slice 2 has not delivered yet (resolved by T7); R2-003 retired inventory duplicated in `agent` and `sddruntime` (fold into T5 single contract inventory); R2-001 doc comment on `verifyClaudeLegacy4RResidue` contradicts `DriftNonOwned` body; R2-002 residue check keys are bare literals without a shared constant; R2-004 pass-through test helper; R3-001 OpenCode engine-level test omits retired `TaskAllows`; R3-002 no negative regression assertion on embedded orchestrator review clauses; R3-003 `observeClaudeLegacy4RResidue` treats non-NotExist stat errors as absent; R3-004 residue Observed string order not proven deterministic. Fixing R2-001, R2-002, R3-001, R3-002, R3-003 needs owner authorization as a small follow-up work unit; the rest fold naturally into slice 2.
- Slice boundary 1 (owner authorized 2026-09-26): PR #768 `feat(jarvis-cli): retire legacy 4R agents and report residue informationally`, branch `feat/issue-767-retire-legacy-4r` → `master`, holds `4114dbae..1ce98ee9` (T1–T4). Linked as "Part of #767" so the merge does not close the issue while slice 2 is pending. Label `type:feature`.
- Slice 2 starts on branch `feat/issue-767-wizard-reset` stacked on `feat/issue-767-retire-legacy-4r` (`stacked-to-main`: its PR targets the slice 1 branch until #768 merges, then `master`). Planned split per the issue: reset core (T5+T6) and TUI step (T7+T8), each its own PR. Reviewed boundary for native assessment: `1ce98ee9`.
- Reset core (T5+T6) delivered as a single bounded-writer pass on `feat/issue-767-wizard-reset`.
  - T5 (`20d6a070`): added `internal/sddruntime/reset_inventory.go` — `ResetSurface`/`ResetSurfaceKind` types and `ResetInventory(platform)`, deriving one ordered slice per platform (Claude: settings.json output_style/status_line/hooks/permissions/default_mode, CLAUDE.md marker block, agents/ directory replaced entirely, sdd-orchestrator.md, statusline-command.sh, output-styles/, skills/, hive-hooks/; OpenCode: opencode.json core keys `default_agent`/`permission`/`agent` replaced entirely and `mcp.hive`/`mcp.context7`, AGENTS.md marker block, sdd-orchestrator.md, skills/, plugins/hive.ts, plugins/skill-registry.ts). Historical literal/token lists (hook command tokens, permission allow/deny literals including the four obsolete force-push deny variants, `defaultMode` Jarvis values) are versioned with era comments. Folded the previously duplicated retired-4R-name lists (R2-003 advisory) into `sddruntime.RetiredClaudeReviewAgentBaseNames`/`RetiredOpenCodeReviewAgentNames`, now the single source `internal/agent/runtime.go` and `internal/sddruntime/verify_opencode.go` both call. Tests: exact Display golden per platform, surface ID uniqueness/platform-consistency, ReplacedEntirely on `claude.agents.directory`/`opencode.settings.core_keys`, retired-name single-source equality, permission-literal near-miss non-matching, hook-token and defaultMode-value exact lists.
  - T6 (`c18b623a`): added `internal/agent/reset.go` (`PlanReset`/`ApplyReset`, read-only vs. mutating, both deriving from the same `sddruntime.ResetInventory` via `resetSurfaceByID`), `internal/agent/hookreset.go` (`removeHookNestedCommandsByTokens`/`filterHookGroupNestedCommands` — a reset-only nested-command filter distinct from the install-path `removeHookEntries` in `hookcleanup.go`, which is untouched), and an `orderedObject.delete` helper added to `jsonmerge.go`. `ApplyReset` snapshots every existing target through `lifecycle.BackupStore.CreateSnapshotOfTargets` (one durable snapshot ID) before any write, keeps an in-process journal of `{path, existed, before-bytes}` per mutation, and on any write failure rolls back every already-applied mutation in reverse order (rewriting prior bytes, or deleting a path that did not exist before), returning `*ResetRestoreError{SnapshotID, UnrecoveredPaths, Cause}`; a failure inside rollback itself is reported via non-empty `UnrecoveredPaths`. Failure injection uses a `resetFileOps` seam (no chmod tricks). Regeneration is explicitly out of scope (doc comment); the wizard's install sequence regenerates after a reset. Advisory fold: reused, not duplicated. Tests in `internal/agent/reset_test.go` (13 cases): absent config dir → empty plan/no writes/no backup dir created; idempotent second run; Claude legacy-4R agent files and OpenCode `agent`/`permission` residue removed; Claude user-added agent and OpenCode extra `agent` entries reported (and removed); unrelated settings.json keys, user's own `outputStyle`/`statusLine`/permission entries/`defaultMode`, and CLAUDE.md content outside markers preserved (semantic JSON / exact text checks); mixed hook group keeps matcher and unrelated nested command, drops only the matching one, second all-Jarvis group dropped entirely, near-miss `subagent-stopwatch` command preserved; failure mid-apply fully rolls back (empty `UnrecoveredPaths`); failure during rollback itself reports the exact unrecovered path; direct rollback-branch test for a path absent before (delete, not rewrite); unsupported platform error; whole-directory recursive removal (skills/ with nested subfolders).
  - Full verification after both commits: `go test ./... -count=1` all green; `go vet ./...` clean; `gofmt -l ./` clean; `go test -cover ./internal/agent ./internal/sddruntime` → `internal/agent` 76.5%, `internal/sddruntime` 89.7%.
- Advisory hardening of the reset core (six findings from the native review of `20d6a070..c18b623a`) delivered as a single bounded-writer pass on `feat/issue-767-wizard-reset`, all fixed (`2b5b28cc`):
  - R3-001 (`internal/agent/reset.go`): `applyResetWithOps` no longer always returns `*ResetRestoreError`. It now returns the plain wrapped cause when rollback fully restored prior state, and `*ResetRestoreError` (snapshot ID + unrecovered paths) only when rollback was incomplete; callers distinguish the two with `errors.As`. Updated the type's doc comment, `Error()` text, and the existing failure-midway test to assert the plain-error case instead of `*ResetRestoreError`.
  - R3-002 (symlinks): `resetFileOps` gained `readLink`/`writeSymlink`; `listResetDirEntriesRecursively` (renamed from `listRegularFilesRecursively`) now reports each entry's `IsSymlink` via `fs.DirEntry.Type()` (Lstat semantics, never following a symlink to descend into a directory). `computeWholeDirectoryDeleteMutations` journals a symlink's target via `os.Readlink` instead of reading through it, and restores it via `os.Symlink` on rollback. `ApplyReset` excludes symlink mutations from the durable `lifecycle.BackupStore` snapshot (that store reads file content via `os.ReadFile`, which would read through the link or fail outright for a dangling one); the in-process journal already carries what a symlink rollback needs. Tests: a file symlink (dangling target, to prove no read-through) and a directory symlink (proves no descent) under `claude.agents.directory`.
  - R3-003 (file mode): `resetFileMutation` gained `beforeMode os.FileMode`, captured by `readFile` (now `os.Lstat` + `os.ReadFile`, returning `(data, existed, mode, err)`) and reused by both the apply write and the rollback restore, instead of a hardcoded `0644`. Test: `statusline-command.sh` written with mode `0755`, rollback triggered by an injected failure on a later mutation, asserts the restored file keeps `0755`.
  - R3-004 (unparseable settings): `computeClaudeSettingsReset`/`computeOpenCodeSettingsReset` now return a wrapped `errResetSettingsUnparseable` naming the file instead of silently treating a parse failure or a non-object top-level value as nothing to reset; `PlanReset`/`ApplyReset` propagate it before any write. Tests for both platforms, plus a check that no backup directory is created and the file is left byte-for-byte unchanged.
  - R3-005 (positive-removal coverage): added tests for Jarvis persona `outputStyle` removed (name derived via `claudeManagedOutputStyleNames()`, not hardcoded), exact managed `statusLine` command removed, `permissions.defaultMode == "bypassPermissions"` removed, OpenCode `AGENTS.md` marker block stripped, `plugins/hive.ts` and `plugins/skill-registry.ts` deleted, `mcp.context7` removed while an unrelated `mcp` server is preserved.
  - R3-006 (duplicate hook-filtering loop): deleted the unused `removeHookNestedCommandsByTokens` in `internal/agent/hookreset.go` (it reimplemented the outer per-event-group loop that `computeClaudeSettingsReset` in `reset.go` already has; nothing called it, not even a test). Kept `filterHookGroupNestedCommands`/`hookCommandMatchesAnyToken` as the single shared implementation both the reset and (via the group-level filter) the settings-reset path call.
  - Full verification after the hardening commit: `go test ./... -count=1` all green; `go vet ./...` clean; `gofmt -l ./` clean.
- T7 (wizard reset step) delivered as a single bounded-writer pass on `feat/issue-767-wizard-reset` (`e371bfce`):
  - Inserted `StepConfigReset` as the new first `Step` (before `StepScope`); `initialWizardStep(m)` returns it only when `len(m.Agents) > 0` (computed by `agent.Detect` in `NewModel`), else `StepScope` exactly as before. Wired into both `NewModel` and the cockpit's `CockpitActionInstall` re-entry path.
  - New `internal/tui/config_reset.go`: `resetAgentInventory` (per detected agent: `DisplayLines` read verbatim from `sddruntime.ResetInventory(platform)`, plus `agent.PlanReset`'s concrete `Plan.Changes` residue, plus `PlanErr`). `initializeConfigResetStep` computes this once from `m.Agents` via the newly exported `agent.PlatformForAgentName` (single source shared with `internal/agent/runtime.go`'s existing `platformForAgent`). `updateConfigReset`/`viewConfigReset` follow the existing `updateScope`/`viewScope` pattern: a two-option selector (No preselected), Up/Down/`j`/`k` toggle, Enter confirms; a `PlanErr` on any agent (e.g. an unparseable settings file) locks navigation and forces `resetConsented = false`, and the view surfaces the error text.
  - Wired acceptance into the apply path: `configureWizardAgents` (`internal/tui/agent_setup.go`) gained `resetConsented bool, home string` parameters; when consented, it calls `agent.ApplyReset` for each agent's resolved platform BEFORE that agent's normal install/merge steps, recording `AgentApplyResult.ResetApplied`/`ResetSnapshotID`, and aborts that agent on a reset failure (including a `*ResetRestoreError`'s snapshot ID and unrecovered paths, surfaced through the ordinary error path). `runAgentConfigSequence` (`internal/tui/steps.go`) passes `m.resetConsented, home` and appends `configResetSummaryLines(results)` to the completion summary — one line per agent naming its snapshot ID; declining (or nothing detected/reset) adds no reset text at all, unchanged from before this step existed. The no-TUI wizard path (`internal/tui/nontui.go`) passes `false, home`: unchanged behavior, no reset prompt there (out of scope for T7).
  - teatest decision: `go.mod` has no teatest dependency and none was added (repository rule against casual dependencies). Followed the existing repo pattern instead — calling `updateConfigReset`/`viewConfigReset` directly and using `isolateTestHome`/`setTestHome` from `internal/tui/test_helpers_test.go`, matching how every other wizard step in this package is already tested.
  - New `internal/tui/config_reset_test.go`: step shown/hidden by detected-agent count (none/Claude-only/OpenCode-only/both, via real `agent.Detect` over an isolated temp `HOME` with `PATH` cleared); `DisplayLines` equal `sddruntime.ResetInventory(claude)` and contain the exact `claude.agents.directory` Display text; navigation (Up/Down/`j`/`k` toggle, Enter confirms, default No on immediate Enter, no-agents Enter skips straight to `StepScope`); a `PlanErr` (unparseable `settings.json`) locks the answer to No and the view names the file; the view shows a short unwrapped contract Display line plus concrete `(user-added)` residue for a real extra agent file; `configureWizardAgents` with `resetConsented:false` leaves seeded legacy-4R residue on disk untouched and `configResetSummaryLines` empty; with `resetConsented:true` it removes the residue, returns a non-empty snapshot ID, and `configResetSummaryLines` produces exactly one line naming the agent and that snapshot ID.
  - Fixed five pre-existing tests that assumed zero detected agents but broke because this sandbox machine has a real `opencode` binary on `PATH` (`~/.opencode/bin/opencode`), which `agent.Detect` already treated as installed before this task: `TestCockpitInstallReconfigureEntersWizard`, `TestNewModel_BlankPersonaAcceptanceBlocksLegacyV1PresetAndPreservesConfig`, `TestNewModel_BlankPersonaAcceptanceBlocksMissingPresetAndPreservesConfig`, `TestNewModel_WithEmptyWizardConfig`, `TestNewModel_NoTUIFallbackStartsWizardNotCockpit` now call `isolateTestHome(t)` + `t.Setenv("PATH", "")`, the same hermetic pattern `TestRunNoTUI_SelectsSkill` already used for the identical reason.
  - Full verification after the T7 commit: `go test ./... -count=1` all green; `go vet ./...` clean; `gofmt -l ./` clean; `go test -cover ./internal/agent ./internal/tui` → `internal/agent` 77.9%, `internal/tui` 85.2%.
- T8 hardening (bounded writer pass on `feat/issue-767-wizard-reset`, commit `5136a42e`): fixed all findings identified against the T7 range `2b5b28cc..07442a19`.
  - **Symlinked top-level surfaces** (R1-001/R4-001/R2-001/R3-mode-from-lstat): `defaultResetFileOps.readFile` took mode from `os.Lstat` but content from `os.ReadFile`, so a symlinked `settings.json`/`opencode.json`/`CLAUDE.md`/`AGENTS.md`/`statusline-command.sh` would have been replaced by a regular file at the link's own Lstat mode. Added `resetFileOps.lstatKind`/`resolveSymlinkTarget` and a new `resolveEditableSurfacePath` helper (`internal/agent/reset.go`): an edit-in-place surface (JSON settings, marker-block) is resolved to its real target first — the mutation journals and writes THAT path, the link itself untouched. `computeWholeFileDeleteMutation` now lstats first and, when the surface itself is a symlink, deletes the link and journals it as `isSymlink` (never touching the target), matching the existing whole-directory-entry behavior. Doc comments on `readFile` corrected to state it is never called directly on a possibly-symlinked path.
  - **Symlink rollback untested** (R3-symlink-rollback-untested): added `TestApplyReset_Claude_SymlinkedTopLevelSurfaces_MidApplyFailureRollsBack` proving a mid-apply failure rolls back both an edited-in-place symlinked settings.json (real target restored, link intact) and a deleted whole-file symlink (link recreated pointing at the same target).
  - **Symlinks excluded from the durable snapshot** (R4-003): added a `<snapshotID>.symlinks.json` sidecar written next to the `lifecycle.BackupStore` manifest (`writeResetSymlinkSidecar`), recording `{path, target}` for every symlink mutation; a write failure there is deliberately swallowed and never fails an otherwise-successful reset (it is auxiliary manual-recovery metadata, not part of the reset's own rollback path).
  - Deviation found and fixed during this task, not in the original finding list: resolving a symlinked settings.json to a target OUTSIDE Jarvis's config roots (a realistic dotfile-manager layout, e.g. `~/dotfiles/claude-settings.json` symlinked into `~/.claude/settings.json`) made `lifecycle.BackupStore.CreateSnapshotOfTargets` refuse the whole reset (its confinement check rejects out-of-root paths). Added `withinBackupAllowedRoots` (mirroring `BackupStore`'s own three allowed roots) so such a target is excluded from the durable snapshot exactly like a symlink mutation — the in-process journal still fully protects it for THIS run; only the separate durable snapshot does not cover it, and this is documented in code, not silently accepted.
  - **Install failure after a consented reset leaves surfaces deleted** (R4-002): `ApplyReset`'s `ResetResult` now carries a `Rollback func() error` closure, valid after return, built from the same in-process journal the apply-time failure path already rolls back from. `configureWizardAgents` (`internal/tui/agent_setup.go`) calls it when that same agent's subsequent install fails, folding a rollback failure into `res.Err` and recording `AgentApplyResult.ResetRolledBack`/`ResetRollbackErr`; an earlier agent that reset and reinstalled successfully is left untouched (loop already stopped at the first per-agent error, unchanged).
  - **Summary text wrong for OpenCode / hardcoded warning paragraph** (R2-002/R2-003): added `replacedEntirelySurfaceDisplays(platform)` (`internal/tui/config_reset.go`) reading `sddruntime.ResetInventory`'s `ReplacedEntirely` surfaces; both `viewConfigReset`'s warning paragraph and `configResetSummaryLines` derive their "entirely replaced" wording from it per detected platform, replacing the old hand-written, Claude-only paragraph.
  - **12 positional parameters** (R2-004): introduced `WizardAgentApplyOptions` (`internal/tui/agent_setup.go`) bundling every `configureWizardAgents` parameter after `agents`; updated the two production call sites (`internal/tui/steps.go`, `internal/tui/nontui.go`) and all nine test call sites mechanically (script-driven, then hand-verified).
  - New tests (`internal/agent/reset_test.go`): symlinked settings.json edited in place with mode preserved from the real target and the link untouched; symlinked whole-file surface removed as a link with the target untouched; the combined mid-apply rollback case above; `ApplyReset.Rollback` undoing a successful reset (and staying nil when there was nothing to reset); the symlink sidecar written/absent as expected; two `lstatKind`-error propagation tests for the new resolution paths. New tests (`internal/tui/config_reset_test.go`): reset-then-install-failure rollback for one agent and for two agents (earlier successful agent stays applied, only the failing agent rolls back, with residue content asserted restored byte-for-byte in both cases); OpenCode-only warning/summary text asserted to name OpenCode's own `ReplacedEntirely` surfaces, never Claude's `agents/` directory.
  - Full verification: `go test ./... -count=1` all green; `go vet ./...` clean; `gofmt -l ./` clean.
- T8 docs (bounded writer pass, commit `24d532de`): added a "Configuration reset (upgrading an existing machine)" section to `docs/getting-started.md` (what it lists per platform verbatim from the contract's intent, what accepting entirely replaces and that it is not recoverable from the running config, the backup snapshot ID in the summary, and that declining/`jarvis sync`/`jarvis reconcile`/doctor never perform it); cross-referenced it from `docs/installation.md`'s new "Upgrading an existing machine" section and from `README.md`'s `jarvis sync` paragraph (explicit "never resets or removes existing configuration" sentence). Updated `docs/maintenance/rdd-experiment-retirement.md`'s existing-machines paragraph now that the reset is delivered, not a later slice, adding the snapshot-ID and auto-rollback-on-failure facts. Added a "Artifact-retiring releases" section to `docs/release-runbook.md` requiring release notes to name a retired/replaced Jarvis-managed artifact so users know to accept the wizard reset. No `CHANGELOG.md` exists in this repository, so no changelog entry was added (verified by `fd -i changelog`). `AGENTS.md`/`CLAUDE.md` needed no parity edit: no operational rule changed, only cross-references into existing docs.
  - Coverage after both T8 commits: `go test -cover ./internal/agent ./internal/sddruntime ./internal/tui` → `internal/agent` 78.1%, `internal/sddruntime` 89.7% (unchanged), `internal/tui` 85.3%. Full `go test ./... -count=1` and `go vet ./...` both clean.
- Slice 2 delivery: split into reset-core and TUI pull requests (owner decision).
- Final hardening pass (bounded writer, commit `c8bde7de` on `feat/issue-767-wizard-reset`) fixed the native-review advisories from `5136a42e..2a9501cc`:
  - **R1-001/R4-002/R3-001/R2-004 (silent durable-snapshot exclusions).** `withinBackupAllowedRoots` (`internal/agent/reset.go`) now canonicalizes both the resolved mutation path and every allowed root through the new `lifecycle.CanonicalizePath` (an exported wrapper of the store's own `canonicalizePath`, with `lifecycle.ErrPathAbsent` exported too), instead of a plain `filepath.Clean` string-prefix compare: a target reached through a symlinked `$HOME`, or an in-root target whose ancestor is a symlink, now resolves to the same canonical form as the corresponding root. The root list itself comes from a new `lifecycle.BackupStore.AllowedRoots()` export — there is no second, hand-copied list to drift, so the "add a parity test" instruction is satisfied by `TestWithinBackupAllowedRoots_DerivesRootsFromBackupStore` (asserts every `AllowedRoots()` entry is itself reported within) plus `TestWithinBackupAllowedRoots_SymlinkedHomeDirectory_StillMatchesRealRoot` (the regression case: a resolved real path matches a root reached only through a symlinked home directory). New `ResetPlan.NotInDurableSnapshot`/`ResetResult.NotInDurableSnapshot` (`[]string`, sorted, deduplicated) list every mutation path a symlink or an out-of-root resolved target excludes, computed by a shared `partitionDurableSnapshotTargets` both `PlanReset` (now takes an added `homeDir` parameter) and `ApplyReset` call, so the plan and the applied result can never disagree. The snapshot-exclusion sidecar (`internal/agent/reset.go`, still `<snapshotID>.symlinks.json`) now records both a symlink mutation (`reason: "symlink"`, with its prior target) and an excluded out-of-root edit-in-place mutation (`reason: "outside_backup_roots"`, path only) — previously it silently omitted the latter. `internal/tui/config_reset.go`'s `viewConfigReset` prints the excluded paths before the Yes/No choice ("These files will only be recoverable through the in-process rollback of this run..."), and `initializeConfigResetStep` now resolves `home` via `os.UserHomeDir()` to pass through to `agent.PlanReset`. Tests: `TestPlanAndApplyReset_NotInDurableSnapshot_IncludesSymlinksAndOutOfRootTargets` (both a whole-file symlink and a marker-block surface resolved outside the allowed roots), `TestViewConfigReset_ShowsNotInDurableSnapshotWarning`.
  - **R4-001/R2-002/R2-003 (rollback coverage and summary suppression).** `configureWizardAgents` (`internal/tui/agent_setup.go`) now keeps each agent's reset-rollback closure on `AgentApplyResult.resetRollback` (unexported) after a successful install, not just during that same agent's own failure branch, so a later shared-step failure (persona profile apply, which runs only after every agent's install already succeeded) can still roll back every agent whose reset is still pending. A new `rollbackAgentReset` helper invokes it once, records `ResetRolledBack`/`ResetRollbackErr`, and returns a description folded into the triggering error. A runtime-verification failure (the final per-agent loop, which runs only after profile apply already succeeded) deliberately does not roll back — the reinstall is complete — matching the task's explicit instruction. `configResetSummaryLines`/`configResetOutcomeSuffix` (`internal/tui/config_reset.go`) were rewritten to never drop the reset line once `ResetApplied` is true, regardless of `Err`: it now reports "entirely replaced ..." (nothing else failed), "rolled it back to its pre-reset configuration" (profile-apply failure), "rollback could not fully restore it: ..." (a `*ResetRestoreError`), or "kept even though a later step failed" (runtime-verification failure). Tests: `TestConfigureWizardAgents_ProfileApplyFailure_RollsBackSucceededAgentsReset` (drives the failure via a `setupAgentStub` that does not implement `persona.ProfileAgent`, so a non-nil `Resolved` reliably fails `applyWizardProfile` without a new stub type), `TestConfigureWizardAgents_RuntimeVerificationFailure_DoesNotRollBackReset`.
  - **R3-003 (post-rollback residual install files).** Chose the smaller of the two options the task offered: a new `agent.ResetManagedDirectories(platform)` (`internal/agent/reset.go`) lists every directory a reset for that platform owns (`whole_directory` surfaces, plus the parent of a nested `whole_file` surface, e.g. `plugins/hive.ts` → `plugins`), and `managedDirectoryRollbackWarning` (`internal/tui/agent_setup.go`) folds those directories (joined with the agent's `ConfigDir()`) into the message `rollbackAgentReset` returns, on both a successful and a failed rollback. This does not extend the journal to snapshot-and-restore whole directories wholesale (the larger option): a rollback still only restores paths its own journal recorded, so a file the subsequent install wrote fresh is not deleted, and the warning names where such a file might remain instead of silently implying full undo. Tests: `TestResetManagedDirectories_Claude`, `TestResetManagedDirectories_OpenCode`.
  - **R2-001/R2-005 (docs hand-write the inventory).** `docs/getting-started.md`'s "What accepting replaces entirely" paragraph now quotes the two `ReplacedEntirely` surfaces' exact contract `Display` text verbatim (`claude.agents.directory` and `opencode.settings.core_keys` — the old text wrongly said only `default_agent`/`agent`, omitting `permission`, because it hand-wrote OpenCode's surface instead of quoting the contract). Added a new "What the durable snapshot does not cover" paragraph documenting the symlink/out-of-root sidecar limit and that recovery is the in-process rollback of that same run, not the durable snapshot afterward. Did not add a doc-vs-contract parity Go test: no existing precedent for a `jarvis-cli` Go test reading a file from the sibling `docs/` directory outside the module, and doing so would add a fragile relative-path dependency on the test's working directory across a module boundary for a two-line contract quote; verified the quoted text is byte-identical to `internal/sddruntime/reset_inventory.go`'s `Display` fields with `rg` instead.
  - Verification: `go test ./... -count=1` (all packages green), `go vet ./...` (clean), `gofmt -l ./` (clean), `go test -cover ./internal/agent ./internal/tui` → `internal/agent` 78.1%, `internal/tui` 85.4%. No advisory from the list was left unaddressed.
- Native reviews of slice 2 (all owner-consented, all approved and acknowledged, boundary advances each time): `review-9ee8ea03a6adbdd4` on `20d6a070..adf9819c` (reliability lens); `review-473f49e60165642f` on `2b5b28cc..07442a19` (four lenses); `review-c8ac7ab79424cac0` on `5136a42e..2a9501cc` (four lenses); `review-1cebb42e72ab6a78` on `c8bde7de..de6057b4` (reliability lens). Reviewed boundary: `de6057b4`.
- Follow-up candidates left open on purpose after the bounded final hardening round (advisory, non-blocking, from `review-1cebb42e72ab6a78`): R3-001 profile-apply failure path discards the `rollbackAgentReset` outcome string, so a rollback failure or the residual-file warning never reaches `Err` (`internal/tui/agent_setup.go` ~287-293); R3-002 `os.UserHomeDir` error ignored in `internal/tui/config_reset.go` ~60, an empty home would canonicalize backup roots against the working directory and could misclassify `NotInDurableSnapshot` in the pre-consent plan; R3-003 a path that canonicalizes to `ErrPathAbsent` is reported as outside the backup roots even when lexically inside one (`internal/agent/reset.go` ~502-503). Each is a small work unit; owner decides whether to fold them before merge or file them.
- Slice 2 delivery split (stacked-to-main): branch `feat/issue-767-reset-core` at `2b5b28cc` holds `240fecf1..2b5b28cc` (T5, T6, first hardening; 9 files, +2303/-16) and targets `feat/issue-767-retire-legacy-4r` until #768 merges; branch `feat/issue-767-wizard-reset` holds `e371bfce..de6057b4` plus this entry (T7, T8, later hardening; 21 files, +2239/-90) and targets `feat/issue-767-reset-core`. Owner authorized opening both: PR #769 `feat(jarvis-cli): add contract-owned configuration reset core` (`feat/issue-767-reset-core` → `feat/issue-767-retire-legacy-4r`) and PR #770 `feat(jarvis-cli): add consented configuration reset step to the wizard` (`feat/issue-767-wizard-reset` → `feat/issue-767-reset-core`), both `type:feature`, bilingual bodies, "Part of #767". Merge order: #768, then #769, then #770; retarget each child to `master` as its predecessor merges.
- T9 local evidence: focused `go test ./internal/agent -run '^TestApplyReset_RollbackRestoresOriginalExecutableBit$' -count=1`, `go test ./... -count=1`, `go vet ./...`, `gofmt -l internal/agent/reset_test.go`, `git diff --check` all exit 0 on Linux; Windows CI not run because commits remain local. Native review approved and acknowledged for the committed core range; no PR push yet.
- T10 local evidence: focused `go test ./internal/agent ./internal/tui -count=1`, `go test ./... -count=1`, `go vet ./...`, `gofmt -l` over five touched Go files, `git diff --check` all exit 0 on Linux; a final comment-only correction followed the verifier's observation. No Windows execution: PR #770 remains at old remote HEAD until owner authorizes push. The TUI viewport test now uses an ample width derived from the actual path rather than assuming a fixed 120 columns.
- T10 native review: `review-e4280c21f41ef76a` approved and acknowledged on `de1065b3..dfd053bf` (one reliability lens); commit `dfd053bf`. No remote delivery authorized by this session; both PRs still show their previous failed Windows jobs.
- Next: push both feature branches (explicit owner authorization), inspect new Windows CI jobs, and report any failure without claiming T9/T10 complete prematurely. T1–T8 remain complete.
