{
  "name": "hive-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "lint": "tsc --noEmit",
    "test": "vitest run --allowOnly=false"
  },
  "devDependencies": {
    "jsdom": "^24.1.3",
    "typescript": "^5.6.3",
    "vite": "^8.0.0",
    "vitest": "^4.0.0"
  },
  "dependencies": {
    "dompurify": "^3.4.12",
    "marked": "^18.0.7"
  }
}
