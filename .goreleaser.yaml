version: 2

project_name: jarvis-dev

before:
  hooks:
    - go mod tidy -C jarvis-cli
    - go mod tidy -C hive-daemon

builds:
  # ============================================================
  # jarvis — user-facing CLI and setup wizard
  # ============================================================
  - id: jarvis
    main: ./cmd/jarvis
    dir: jarvis-cli
    binary: jarvis
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

  # ============================================================
  # hive — local governance CLI
  # ============================================================
  - id: hive
    main: ./cmd/hive
    dir: jarvis-cli
    binary: hive
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

  # ============================================================
  # hive-daemon — local MCP server
  # ============================================================
  - id: hive-daemon
    main: ./cmd/hive-daemon
    dir: hive-daemon
    binary: hive-daemon
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  # Separate archives for each binary.
  - id: jarvis-archive
    ids:
      - jarvis
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "jarvis_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

  - id: hive-daemon-archive
    ids:
      - hive-daemon
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "hive-daemon_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

  - id: hive-archive
    ids:
      - hive
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "hive_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

release:
  github:
    owner: Thrasno
    name: jarvis-ai-devs
  draft: false
  prerelease: auto
  name_template: "{{ .Tag }}"
