# Tasks: Bounded, Verifiable Apply Progress

## Review Workload Forecast

| Field | Value |
| ------- | ------- |
| Estimated changed lines | 6,280–6,785 total after PR 6A–6E: verified completed PR 1–5 cumulative source delta was 4,960; PR 6A: 300–390; PR 6B: 320–395; PR 6C: 260–380; PR 6D: 180–300; corrective PR 6E: 260–360. Historical PR 4A1 correction remained 465–550 under its explicit 550-line `size:exception` (`sha256:5502dd03b2d991ca4dc580e6d7670700b5af03faca5d4acce0824990f21e58d7`); superseded combined PR 4B2 measured 503 and failed verification. |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | Historical PR 1A → PR 1B → PR 2 → PR 3 → PR 4A1 → PR 4A2 → PR 4B1 → PR 4B2A → PR 4B2B → PR 5; completion chain PR 6A pure planner → PR 6B OpenSpec checkpoint → PR 6C Hive/hybrid reuse → PR 6D assets, audit reconciliation, and final scenarios → PR 6E stream-digest recovery binding and re-verification |
| Delivery strategy | ask-on-risk → chained delivery selected |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

### Reviewable Work Units

| Unit | Dependency / boundary | Finish, verification, rollback |
| --- | --- | --- |
| PR 1A | Independent protocol model/canonicalization foundation | Canonical bounded documents; `hivederive` focused tests/vet; revert package. |
| PR 1B | PR 1A; protocol validation and legacy conversion | Validated task coverage and migration; `hivederive` tests/vet; revert validation layer. |
| PR 2 | PR 1B; one inseparable CAS unit | Public atomic advance; daemon tests/vet; disable route, retain rows. |
| PR 3 | PR 2 | Hive reads block invalid lifecycle state; CLI/daemon tests; revert routing only. |
| PR 4A1 | PR 1–3; OpenSpec durability primitives only | Internal atomic snapshot/batch/receipt file store with behavior-first `t.TempDir()` tests; focused/module CLI tests and vet; revert the internal writer while retaining any topology it created. |
| PR 4A2 | PR 4A1; public OpenSpec advance adapter only | `jarvis sdd progress advance` delegates to PR 4A1 and command-level CAS/idempotent crash-retry integration tests pass; revert the adapter/tests without duplicating or removing durability primitives. |
| PR 4B1 | PR 4A; first replacement for the unstarted PR 4B | Independently validated OpenSpec reads, fail-closed hybrid comparison and lifecycle blocking, and full-topology archive movement; CLI tests/vet; revert resolver/lifecycle/archive routing while retaining v2 topology. |
| PR 4B2A | PR 4B1; replaces the 503-line combined PR 4B2 that failed verification | Durable hybrid receipt recovery and production store-mode/CLI mutation routing; both interruption directions, replay, stale conflict, request conflict, and no committed-side rewrite; CLI tests/vet; revert only coordinator/routing while retaining topology and receipts. |
| PR 4B2B | PR 4B2A; independently reviewable legacy slice | Conservative legacy read validation and next-authorized-mutation v2 upgrade; read-only commands make no writes and failed upgrade preserves source/retryability; CLI tests/vet; revert upgrade routing while preserving legacy authority. |
| PR 5 | PR 1–4B2B | Sources emit only supported protocol; catalog tests; revert assets first. |
| PR 6A | PR 1–5; no persistence or CLI | Pure one-batch maximal-prefix planner and all four dispositions; hivederive tests/vet; remove planner files. |
| PR 6B | PR 6A; OpenSpec only | Sibling checkpoint delegates one planned prefix to existing OpenSpec `Advance`; durable 162,725-rune scenario; remove command seam, retain v2 topology. |
| PR 6C | PR 6B; configured Hive/hybrid only | Reuse resolver/advance/CAS/receipt paths with continuation/capacity propagation; CLI tests/vet; revert checkpoint routing only. |
| PR 6D | PR 6C | Canonical asset/catalog guidance, audit-store reconciliation, and cross-mode final scenarios; revert assets first, then checkpoint coordinator, never committed data. |
| PR 6E | PR 6A–6D; corrective only | Bind the full ordered entry stream with a deterministic continuation digest; reject changed, reordered, truncated, or extended resumes before planning/storage; rerun final verification and mirrors. Revert only the high-level checkpoint binding/guidance, retaining committed data and low-level advance boundaries. |

## 1A. Protocol Models and Canonicalization (PR 1A)

- [x] 1A.1 **RED:** Add table-driven failures in `hivederive/applyprogress/canonical_test.go` for typed v2 models, canonical bytes, deterministic SHA-256, valid IDs, and exact 40,000-rune Unicode boundaries. <!-- sdd-owner: implementation -->
- [x] 1A.2 **GREEN:** Create `hivederive/applyprogress/{model,canonical}.go` with v2 structs, deterministic JSON/SHA-256 sealing, ID checks, and typed capacity outcomes. <!-- sdd-owner: implementation -->
- [x] 1A.3 **TRIANGULATE/REFACTOR:** Cover non-canonical JSON, invalid UTF-8/IDs, oversized evidence/snapshot documents, and hash stability; simplify only after `cd hivederive && go test ./... && go vet ./...`. <!-- sdd-owner: implementation -->

## 1B. Protocol Validation and Legacy Conversion (PR 1B)

- [x] 1B.1 **RED:** Add table-driven failures in `hivederive/applyprogress/{validate,legacy}_test.go` for manifest IDs/digest, order, coverage, duplicate/missing/corrupt evidence, and ambiguous legacy input. <!-- sdd-owner: implementation -->
- [x] 1B.2 **GREEN:** Create `hivederive/applyprogress/{validate,legacy}.go` with task normalization/digests, typed validation outcomes, ordered coverage validation, and conservative conversion. <!-- sdd-owner: implementation -->
- [x] 1B.3 **TRIANGULATE/REFACTOR:** Add malformed/reordered/orphan and complete-vs-partial cases; run `cd hivederive && go test ./... && go vet ./...`. <!-- sdd-owner: implementation -->

## 2. Guarded Hive Advance (PR 2; cohesive size exception candidate)

- [x] 2.1 **RED:** In `hive-daemon/internal/db/apply_progress_test.go` and boundary tests, specify stale writers, identical retry, request-ID reuse conflict, batch collision, and transport recovery. <!-- sdd-owner: implementation -->
- [x] 2.2 **GREEN:** Add head/receipt migration in `internal/db/db.go` and transactional `apply_progress.go`; wire typed GET/POST and MCP advance/get through `db/sdd.go`, governance, HTTP, MCP, and `jarvis-cli/internal/hiveclient/client.go`, never `mem_save`. <!-- sdd-owner: implementation -->
- [x] 2.3 **TRIANGULATE/REFACTOR:** Exercise concurrent SQLite writers and HTTP/MCP 200/409/413/422/503 outcomes; run `cd hive-daemon && go test ./... && go vet ./...` and focused CLI client tests. <!-- sdd-owner: implementation -->

## 3. Hive Resolution and Lifecycle Blocking (PR 3)

- [x] 3.1 **RED:** Add `hive-daemon/internal/db/sdd_test.go` and `jarvis-cli/internal/sddstatus/{source,status}_test.go` cases for missing/corrupt batches, manifest mismatch, continuation/conflict, and blocked apply/status/verify/archive. <!-- sdd-owner: implementation -->
- [x] 3.2 **GREEN:** Resolve only snapshot-referenced batches in daemon retrieval; update `source.go`, `status.go`, and `cmd/jarvis/cmd_sdd.go` to preserve typed fail-closed outcomes. <!-- sdd-owner: implementation -->
- [x] 3.3 **TRIANGULATE/REFACTOR:** Cover delayed/forked synced rows and valid partial/complete routing; run `cd jarvis-cli && go test ./... && go vet ./...` plus daemon focused tests. <!-- sdd-owner: implementation -->

## 4A1. Atomic OpenSpec File-Store Durability Primitives (PR 4A1 correction; explicit 550-line `size:exception`)

**Start / dependency:** PR 1–3 are integrated; this is the first ordered replacement for the clean 391-line provisional PR 4A attempt, which stopped before required triangulation and retained no source. **Finish:** an internal OpenSpec file store makes a v2 snapshot authoritative only after its exact immutable batches and request receipt are durable; a simulated pre-rename interruption leaves the prior snapshot authoritative and can be retried through the internal store exactly once. **Out of scope:** every public CLI command/argument/output concern, hybrid comparison/repair, legacy upgrade, archive, and source assets. **Verification:** narrow `internal/sddprogress` tests, then `cd jarvis-cli && go test ./... && go vet ./...`. **Rollback:** remove the internal OpenSpec writer while retaining `apply-progress.md`, `apply-evidence/`, and `.apply-progress-receipts/`; readers continue to treat any retained topology conservatively.

- [x] 4A1.1 **RED:** In `jarvis-cli/internal/sddprogress/{openspec,store}_test.go`, write behavior-first `t.TempDir()` cases for create-only byte-identical `apply-evidence/<batch-id>.json` reuse versus collision, expected generation/digest conflict, and receipt/batch durability followed by an injected interruption before snapshot rename. <!-- sdd-owner: implementation -->
- [x] 4A1.2 **GREEN:** Create the internal OpenSpec store in `jarvis-cli/internal/sddprogress/{store,openspec}.go` with a change-local `apply-progress.lock`, create-only immutable batch equality checks, `.apply-progress-receipts/<request-id>.json` persistence, expected-state recheck, fsynced temporary `apply-progress.md` rename, and directory fsync; expose only a Go-level advance seam for PR 4A2. <!-- sdd-owner: implementation -->
- [x] 4A1.3 **TRIANGULATE/REFACTOR:** Extend `jarvis-cli/internal/sddprogress/{openspec,store}_test.go` for request-ID payload conflict, same-request retry after the injected interruption, and old-snapshot authority; keep the complete PR 4A1 correction delta at or below the explicitly authorized 550 additions plus deletions (`sha256:5502dd03b2d991ca4dc580e6d7670700b5af03faca5d4acce0824990f21e58d7`) and run the stated focused/module tests and vet. <!-- sdd-owner: implementation -->

## 4A2. Public OpenSpec Advance Adapter and Integration (PR 4A2; 170–240 changed lines, hard 400-line budget)

**Start / dependency:** reviewed PR 4A1 durability primitives are available; this unit calls their Go-level advance seam and does not recreate locking, receipt, batch, fsync, or rename behavior. **Finish:** the public `jarvis sdd progress advance` adapter submits OpenSpec requests through PR 4A1 and command-level end-to-end tests prove CAS conflict handling plus idempotent same-request crash-retry without duplicate publication. **Out of scope:** modifications to `jarvis-cli/internal/sddprogress/{store,openspec}.go` durability semantics, hybrid comparison/repair, legacy upgrade, archive, and source assets. **Verification:** narrow command and `internal/sddprogress` tests, then `cd jarvis-cli && go test ./... && go vet ./...`. **Rollback:** remove only `cmd_sdd_progress.go` adapter wiring and its command-level tests; preserve the PR 4A1 internal store and all durable topology unchanged.

- [x] 4A2.1 **RED:** Add `jarvis-cli/cmd/jarvis/cmd_sdd_progress_test.go` end-to-end `t.TempDir()` cases that invoke the public `jarvis sdd progress advance` command path against PR 4A1: stale expected generation/digest returns the typed conflict/current state, and a retry with the same request ID after an injected pre-rename interruption commits one snapshot without duplicating evidence. <!-- sdd-owner: implementation -->
- [x] 4A2.2 **GREEN:** Implement `jarvis-cli/cmd/jarvis/cmd_sdd_progress.go` as the public `jarvis sdd progress advance` adapter that constructs the typed OpenSpec request, calls the PR 4A1 store seam, and renders committed, conflict, and recovery outcomes without performing direct filesystem durability work. <!-- sdd-owner: implementation -->
- [x] 4A2.3 **TRIANGULATE/REFACTOR:** In `jarvis-cli/cmd/jarvis/cmd_sdd_progress_test.go` and the reused `jarvis-cli/internal/sddprogress/{openspec,store}_test.go` harness, prove identical retry is idempotent, changed-payload request-ID reuse fails closed, and a crash-retry preserves old-snapshot authority until publication; keep the complete PR 4A2 delta below 400 additions plus deletions and run the stated focused/module tests and vet. <!-- sdd-owner: implementation -->

## 4B1. Validated OpenSpec Reads, Hybrid Blocking, and Full-Topology Archive (PR 4B1; 260–360 changed lines, hard 400-line budget)

**Start / dependency:** reviewed PR 4A OpenSpec durability/publication and PR 4A2 public adapter are available; this is the first ordered replacement for the unstarted PR 4B attempt. **Finish:** OpenSpec reads validate the authoritative snapshot and exactly referenced batches; independently validated OpenSpec and Hive states must agree before lifecycle use; missing, invalid, or different states return `backend_diverged` and block apply/status/verify/archive without a winner; a successful archive moves the complete OpenSpec change topology together. **Out of scope:** durable missing-side publication retry, receipt-state repair, next-mutation legacy upgrade, PR 4A durability semantics, and executor source assets. **Verification:** focused `internal/sddprogress` and `cmd/jarvis` tests, then `cd jarvis-cli && go test ./... && go vet ./...`. **Rollback:** remove OpenSpec/hybrid resolver, lifecycle-blocking, and archive-routing changes; retain every existing v2 snapshot, `apply-evidence/`, `.apply-progress-receipts/`, and legacy source unchanged.

- [x] 4B1.1 **RED:** Add table-driven and `t.TempDir()` failures in `jarvis-cli/internal/sddprogress/{openspec,hybrid,store}_test.go` for OpenSpec missing/corrupt/reordered/mismatched referenced batches and harmless orphan batches; independently valid-but-different, missing, and invalid Hive/OpenSpec states; and the lifecycle call sites discovered at `jarvis-cli/cmd/jarvis/cmd_sdd.go` that must block apply, status, verify, and archive on `backend_diverged`. Add an archive call-site test proving the snapshot, `apply-evidence/`, and `.apply-progress-receipts/` move with the OpenSpec change. <!-- sdd-owner: implementation -->
- [x] 4B1.2 **GREEN:** Implement validated OpenSpec resolution and independent fail-closed comparison in `jarvis-cli/internal/sddprogress/{openspec,hybrid}.go`; update the concrete lifecycle/archive routing discovered in `jarvis-cli/cmd/jarvis/cmd_sdd.go` so no backend is selected on divergence and archive validates before moving the whole change directory/topology atomically. <!-- sdd-owner: implementation -->
- [x] 4B1.3 **TRIANGULATE/REFACTOR:** Prove valid equal states proceed, neither backend wins on disagreement, invalid OpenSpec references remain non-authoritative, all lifecycle operations return the typed blocking reason, and archived topology is resolvable after the directory move in `jarvis-cli/internal/sddprogress/{openspec,hybrid,store}_test.go` and the discovered `cmd_sdd.go` tests; keep the complete PR 4B1 delta at or below 400 additions plus deletions and run the stated focused/module tests and vet. <!-- sdd-owner: implementation -->

## 4B2A. Durable Hybrid Missing-Side Recovery and Production Mutation Routing (PR 4B2A; 260–340 changed lines, hard 400-line budget)

**Start / dependency:** reviewed PR 4B1 validated reads, divergence blocking, and archive routing are available; this replaces the combined PR 4B2 measured at 503 changed lines that failed verification. **Finish:** a durable hybrid receipt records `pending|committed|failed` per-backend outcomes; the production store-mode selection and public mutating CLI route use the hybrid coordinator; and a same-request replay repairs only the receipt-recorded missing side. Both OpenSpec-then-interrupted-Hive and Hive-then-interrupted-OpenSpec publication directions remain blocked until recovery, without rewriting the already committed side. **Out of scope:** legacy parser/read validation or upgrade, changes to PR 4B1 comparison/lifecycle/archive policy, backend-winner selection, PR 4A durability semantics, and executor source assets. **Verification:** focused `jarvis-cli/internal/sddprogress/{hybrid,openspec,store}_test.go` and `jarvis-cli/cmd/jarvis/cmd_sdd_progress_test.go`, then `cd jarvis-cli && go test ./... && go vet ./...`. **Rollback:** remove only hybrid receipt-recovery and production store-mode/CLI mutation routing; retain committed snapshots, immutable batches, and receipts, leave any partially published state fail-closed, and never rewrite a committed side during rollback.

- [x] 4B2A.1 **RED:** Add table-driven and `t.TempDir()` failures in `jarvis-cli/internal/sddprogress/{hybrid,openspec,store}_test.go` and production command-path cases in `jarvis-cli/cmd/jarvis/cmd_sdd_progress_test.go` for durable per-backend `pending|committed|failed` receipt transitions; OpenSpec committed with Hive interrupted and Hive committed with OpenSpec interrupted; same-request replay limited to the recorded missing side; stale expected generation/digest committing neither side; and changed-payload request-ID reuse failing closed. <!-- sdd-owner: implementation -->
- [x] 4B2A.2 **GREEN:** Implement receipt-backed hybrid missing-side recovery in `jarvis-cli/internal/sddprogress/hybrid.go` with persistence through `jarvis-cli/internal/sddprogress/{store,openspec}.go`; route the production store-mode decision and only the mutating public command in `jarvis-cli/cmd/jarvis/cmd_sdd_progress.go` through that coordinator, preserving receipt state and bypassing already committed batch/snapshot writes on replay. <!-- sdd-owner: implementation -->
- [x] 4B2A.3 **TRIANGULATE/REFACTOR:** Prove both interruption directions return `backend_diverged` until same-request recovery, replay makes the stores equal without a committed-side rewrite, stale requests leave receipt/topology unchanged, and request-ID payload conflicts remain fail-closed; keep the complete PR 4B2A delta at or below 400 additions plus deletions and run the stated focused/module tests and vet. <!-- sdd-owner: implementation -->

## 4B2B. Conservative Legacy Reads and Next-Authorized-Mutation Upgrade (PR 4B2B; 180–250 changed lines, hard 400-line budget)

**Start / dependency:** reviewed PR 4B2A hybrid coordinator and production mutation routing are available; this is a separately reviewable replacement for the legacy portion of the failed 503-line combined PR 4B2. **Finish:** status, verify, and archive conservatively validate legacy cumulative progress without writes; only the next authorized mutating apply upgrades unambiguous legacy evidence to initial v2 progress; and every conversion or commit failure leaves the original legacy source authoritative and retryable. **Out of scope:** hybrid receipt recovery/store-mode routing, changes to PR 4B1 comparison/lifecycle/archive policy, backend-winner selection, PR 4A durability semantics, and executor source assets. **Verification:** focused legacy/resolver tests under `jarvis-cli/internal/sddprogress/` plus `jarvis-cli/cmd/jarvis/{cmd_sdd,cmd_sdd_progress}_test.go`, then `cd jarvis-cli && go test ./... && go vet ./...`. **Rollback:** remove only next-mutation upgrade routing; preserve the legacy source and any committed v2 topology, keep uncommitted upgrade attempts non-authoritative, and retain retryability from the unchanged legacy artifact.

- [x] 4B2B.1 **RED:** Add table-driven and `t.TempDir()` failures in the legacy resolver discovery target under `jarvis-cli/internal/sddprogress/`, `jarvis-cli/cmd/jarvis/cmd_sdd_test.go`, and `jarvis-cli/cmd/jarvis/cmd_sdd_progress_test.go` for valid versus malformed/ambiguous legacy progress; read-only status, verify, and archive making no snapshot, evidence, receipt, or source writes; and the next authorized mutating apply attempting conversion only after successful read validation. <!-- sdd-owner: implementation -->
- [x] 4B2B.2 **GREEN:** Implement conservative legacy resolution in the concrete legacy resolver under `jarvis-cli/internal/sddprogress/`; wire `jarvis-cli/cmd/jarvis/cmd_sdd.go` read-only lifecycle paths to it without mutation; and route only the next authorized mutation in `jarvis-cli/cmd/jarvis/cmd_sdd_progress.go` through bounded v2 conversion and guarded commit, removing or superseding the legacy source only after all required v2 publication commits succeed. <!-- sdd-owner: implementation -->
- [x] 4B2B.3 **TRIANGULATE/REFACTOR:** Prove malformed or ambiguous legacy input fails closed, read-only lifecycle commands perform no writes, a successful upgrade commits initial v2 generation, and conversion, validation, OpenSpec, Hive, or hybrid commit failure preserves the original readable legacy source so the next authorized mutation can retry from it; keep the complete PR 4B2B delta at or below 400 additions plus deletions and run the stated focused/module tests and vet. <!-- sdd-owner: implementation -->

## 5. Executor Sources (PR 5)

- [x] 5.1 **RED:** Extend `jarvis-cli/internal/skills/catalog_contract_test.go` to require batch append, guarded advance, receipt retry, and continuation/conflict/migration recovery guidance. <!-- sdd-owner: implementation -->
- [x] 5.2 **GREEN:** Update source assets `embed/skills/{sdd-apply,sdd-archive}/SKILL.md` and `embed/orchestrator/sdd-orchestrator.md`; do not edit generated machine files. <!-- sdd-owner: implementation -->
- [x] 5.3 **TRIANGULATE/REFACTOR:** Reject legacy cumulative-merge instructions and run `cd jarvis-cli && go test ./... && go vet ./...`; format changed Go with `gofmt`. <!-- sdd-owner: implementation -->

## 6. Final-Verification Checkpoint Completion (Stacked PR 6A → PR 6B → PR 6C → PR 6D → corrective PR 6E)

**Completion boundary:** This amendment closes only the final-verification checkpoint-planning gap. PR 1–5, their completed checkboxes, verification evidence, rollback boundaries, and the parent review note remain historical record. Each PR below has a hard **400 changed-line (additions plus deletions)** cap; if an honest slice exceeds it, pause for a new delivery decision—do not infer a `size:exception`.

**Allowed conceptual surfaces (and no others):**

| PR | Allowed production and test surfaces |
| --- | --- |
| PR 6A | `hivederive/applyprogress/checkpoint.go` and `hivederive/applyprogress/checkpoint_test.go` only; reuse existing model, canonical, and validation code. |
| PR 6B | `jarvis-cli/cmd/jarvis/cmd_sdd_progress.go`, `jarvis-cli/cmd/jarvis/cmd_sdd_progress_test.go`, and focused existing seams/tests under `jarvis-cli/internal/sddprogress/` only. |
| PR 6C | The PR 6B command/test surfaces plus focused existing resolver/advance seams and tests under `jarvis-cli/internal/sddprogress/` and `jarvis-cli/internal/hiveclient/`; no Hive wire-model change. |
| PR 6D | `jarvis-cli/embed/skills/sdd-apply/SKILL.md`, `jarvis-cli/embed/orchestrator/sdd-orchestrator.md`, `jarvis-cli/internal/skills/catalog_contract_test.go`, focused checkpoint tests, and the SDD audit artifacts `openspec/changes/issue-653-bounded-apply-progress/apply-progress.md` and Engram topics for this change. |
| PR 6E | `hivederive/applyprogress/checkpoint.go`, `hivederive/applyprogress/checkpoint_test.go`, `jarvis-cli/cmd/jarvis/cmd_sdd_progress.go`, `jarvis-cli/cmd/jarvis/cmd_sdd_progress_test.go`, `jarvis-cli/embed/skills/sdd-apply/SKILL.md`, `jarvis-cli/embed/orchestrator/sdd-orchestrator.md`, `jarvis-cli/internal/skills/catalog_contract_test.go`, and final audit mirrors only. No low-level advance, storage, daemon, HTTP, MCP, or Hive client wire surfaces. |

**Explicit exclusions:** do not change `jarvis sdd progress advance` behavior or flags; do not create an alternate persistence path, storage table/file, receipt, planner, schema, daemon endpoint, HTTP/MCP endpoint or schema, Hive client wire model, general sync, lifecycle, archive, or legacy-format behavior. The daemon's generic defensive `capacity` remains unchanged. A checkpoint attempt plans and delegates at most one new batch/snapshot generation; it never performs an unbounded hidden commit loop.

### 6A. Shared Pure Boundary Planner (PR 6A; 300–390 changed lines, hard 400-line budget)

**Start / dependency:** PR 1–5 provide the authoritative v2 models, sealing, validation, and capacity limit; no CLI or backend behavior is changed. **Finish:** `hivederive/applyprogress` exposes one I/O-free planner that validates the base/task/cursor input, seals at most one immutable batch and successor snapshot, and chooses the maximal whole-entry prefix. Its exact dispositions are `committed`, nonterminal `continuation_required`, terminal `evidence_item_too_large`, and terminal `snapshot_capacity_exhausted`. **Verification:** `cd hivederive && go test ./applyprogress -count=1`, then `cd hivederive && go test ./... && go vet ./...`. **Rollback:** remove only `checkpoint.go` and `checkpoint_test.go`; no stored topology, receipt, CLI, or backend changes exist to undo.

- [x] 6A.1 **RED:** Add table-driven `hivederive/applyprogress/checkpoint_test.go` cases for base/task-manifest and cursor (`entry_index`, `entry_id`) validation; maximal complete-entry prefix selection; no entry split, mutation, reorder, or hidden batch; exact `committed`, nonterminal `continuation_required` with exclusive next coordinate, and both terminal no-write capacity dispositions. Include an exact 162,725-Unicode-rune stream of multiple complete, individually fitting entries and repeatedly plan from each returned cursor, asserting every planned canonical batch/snapshot is at most 40,000 runes and recovered entries retain exact order/content. <!-- sdd-owner: implementation -->
- [x] 6A.2 **GREEN:** Add `hivederive/applyprogress/checkpoint.go` with a pure `PlanInput`/plan-result contract that reuses existing canonical sealing, manifest coverage, IDs, and validation; it produces one sealed batch and successor snapshot or the exact terminal capacity disposition, with no I/O, locking, ID generation, request handling, retry, backend selection, or CLI imports. <!-- sdd-owner: implementation -->
- [x] 6A.3 **TRIANGULATE/REFACTOR (completed by PR 6E):** The recorded PR 6A tests covered malformed cursor pairs but not reordered-resume rejection. Corrective PR 6E now binds the complete ordered stream and tests reorder, modification, truncation, and extension failures while preserving PR 6A–6D as integrated historical work. <!-- sdd-owner: implementation -->

### 6B. OpenSpec Sibling Checkpoint Command (PR 6B; 320–395 changed lines, hard 400-line budget)

**Start / dependency:** reviewed PR 6A planner is available; the existing OpenSpec resolver and `OpenSpec.Advance` remain the only write path. **Finish:** sibling `jarvis sdd progress checkpoint` accepts the canonical stable request (identity, tasks, nullable base snapshot, expected coordinates, request ID, batch ID, ordered entries, and cursor), verifies the resolved current state/base, delegates one planner result through the existing `sddprogress.AdvanceRequest`, and renders committed/continuation/capacity results. `advance` remains unmodified. **Verification:** focused `cmd/jarvis` and `internal/sddprogress` tests, then `cd jarvis-cli && go test ./... && go vet ./...`. **Rollback:** remove only checkpoint command/coordinator seams and tests; retain all committed v2 snapshots, evidence, receipts, and existing low-level `advance` behavior.

- [x] 6B.1 **RED:** Add `jarvis-cli/cmd/jarvis/cmd_sdd_progress_test.go` `t.TempDir()` command-path cases for canonical checkpoint request/output parsing, initial nullable base and resolved-current/base/expected-coordinate checks, exactly one planned prefix per invocation, and OpenSpec propagation of `committed`, `continuation_required` (committed state, receipt, `next_entry_index`, `next_entry_id`), `evidence_item_too_large`, and `snapshot_capacity_exhausted`; assert either capacity result calls no advance and creates no receipt, batch, snapshot, or coverage. <!-- sdd-owner: implementation -->
- [x] 6B.2 **GREEN:** Add the sibling `checkpoint` adapter in `jarvis-cli/cmd/jarvis/cmd_sdd_progress.go` and only the focused existing `jarvis-cli/internal/sddprogress/` seam needed to resolve validated OpenSpec current state and submit the planner-produced `AdvanceRequest`; preserve the original request ID/batch ID/base/cursor/entries for an exact retry, create a new request ID/batch ID only for a returned continuation, and leave `advance` flags, behavior, and generic defensive capacity handling untouched. <!-- sdd-owner: implementation -->
- [x] 6B.3 **TRIANGULATE/REFACTOR:** Prove a repeated durable OpenSpec command scenario over exactly 162,725 evidence runes: every non-final invocation commits a non-empty maximal prefix and returns matching next index/ID; replaying one uncertain invocation with byte-identical payload returns the same receipt/state without duplicate batch, coverage, or generation; each successor uses returned coordinates with new IDs; the final invocation returns `committed`; resolving recovers every entry once/in order with documents at or below 40,000 runes. Also prove stale CAS and changed-payload request-ID reuse fail closed; run focused/module tests and vet while keeping PR 6B at or below 400 changed lines. <!-- sdd-owner: implementation -->

### 6C. Configured Hive and Hybrid Checkpoint Reuse (PR 6C; 260–380 changed lines, hard 400-line budget)

**Start / dependency:** reviewed PR 6B owns the checkpoint request/result contract and OpenSpec route. **Finish:** the same coordinator selects the configured validated Hive or hybrid resolver and reuses existing Hive `AdvanceApplyProgress` or `Hybrid.Advance`; it propagates committed/continuation coordinates and the two planner capacity outcomes without adding a daemon planner or persistence path. **Verification:** focused `cmd/jarvis`, `internal/sddprogress`, and `internal/hiveclient` tests, then `cd jarvis-cli && go test ./... && go vet ./...`. **Rollback:** revert only checkpoint store-mode routing and its tests; retain existing CAS, immutable batches, durable receipts, OpenSpec/Hive topology, and hybrid recovery data without rewriting either committed side.

- [x] 6C.1 **RED:** Add focused configured-Hive and configured-hybrid checkpoint tests in `jarvis-cli/cmd/jarvis/cmd_sdd_progress_test.go` and concrete existing test seams under `jarvis-cli/internal/{sddprogress,hiveclient}/` for current-snapshot/base rebasing, exact advance-payload retry, and propagation of committed state/receipt, continuation cursor, and both no-write planner capacity outcomes; assert HTTP/MCP and low-level `advance` still expose only their unchanged generic defensive capacity contract. <!-- sdd-owner: implementation -->
- [x] 6C.2 **GREEN:** Extend only the checkpoint coordinator/store-mode selection in `jarvis-cli/cmd/jarvis/cmd_sdd_progress.go` and focused existing resolver seams so Hive calls existing `AdvanceApplyProgress` and hybrid calls existing `Hybrid.Advance` after validated resolution; pass the exact planner output and retain CAS/current-state, request-ID, receipt, legacy, and divergence envelopes rather than translating or duplicating them. <!-- sdd-owner: implementation -->
- [x] 6C.3 **TRIANGULATE/REFACTOR:** Prove same-request replay repairs only the receipt-recorded missing hybrid side for both OpenSpec→Hive and Hive→OpenSpec interruptions, with no committed-side rewrite or cursor advance until equality; prove stale/rebased coordinates and request-ID payload conflicts commit nothing; prove continuation/capacity output parity across OpenSpec, Hive, and hybrid; run focused/module tests and vet while keeping PR 6C at or below 400 changed lines. <!-- sdd-owner: implementation -->

### 6D. Canonical Guidance, Audit Reconciliation, and Final Scenarios (PR 6D; 180–300 changed lines, hard 400-line budget)

**Start / dependency:** reviewed PR 6A–6C make `checkpoint` the high-level bounded writer while low-level `advance` remains compatible. **Finish:** canonical executor sources guide normal use of `checkpoint` and exact continuation/retry/cursor rules; catalog contracts reject stale `advance`-as-planner guidance; audit records reflect PR 6 evidence and the synchronized task/apply-progress state; final scenarios demonstrate the completed contract without generated-machine or daemon changes. **Verification:** focused catalog/command/checkpoint tests, then `cd hivederive && go test ./... && go vet ./...` and `cd jarvis-cli && go test ./... && go vet ./...`. **Rollback:** restore canonical guidance first, then remove the checkpoint coordinator only if necessary; never delete, downgrade, rewrite, or garbage-collect committed snapshots, batches, or receipts.

- [x] 6D.1 **RED:** Extend `jarvis-cli/internal/skills/catalog_contract_test.go` and focused command tests to require canonical `checkpoint` guidance for ordered complete entries, stable base/cursor, same-request byte-identical retry, new request/batch IDs only after continuation, and the exact four checkpoint outcomes; reject guidance that makes `advance`, HTTP, or MCP select prefixes or plan capacity. <!-- sdd-owner: implementation -->
- [x] 6D.2 **GREEN:** Update only `jarvis-cli/embed/skills/sdd-apply/SKILL.md` and `jarvis-cli/embed/orchestrator/sdd-orchestrator.md` to use `jarvis sdd progress checkpoint` for normal executor checkpoints and preserve low-level `advance` recovery/compatibility guidance; after PR 6A–6C evidence is complete, reconcile `openspec/changes/issue-653-bounded-apply-progress/apply-progress.md` and the change's Engram task/apply-progress mirrors with the new PR 6 records, without editing generated user-machine artifacts. <!-- sdd-owner: implementation -->
- [x] 6D.3 **TRIANGULATE/REFACTOR:** Run final cross-mode scenario tests for exact 162,725-rune repeated checkpoints, all four checkpoint outcomes, stale/request-ID/cursor failure no-writes, OpenSpec/Hive/hybrid continuation parity, unchanged `advance`/HTTP/MCP compatibility, and recovered ordered evidence; run the stated focused/module tests and vet, record the commands/results in the PR 6 audit evidence, and keep the complete PR 6D diff at or below 400 changed lines. <!-- sdd-owner: implementation -->

### 6E. Stream-Digest Recovery Binding and Final Re-verification (Corrective PR 6E; 260–360 changed lines, hard 400-line budget)

**Start / dependency:** PR 6A–6D remain approved and integrated; final verification found only the missing recovery binding described by incomplete task 6A.3. **Finish:** the first checkpoint deterministically digests the entire ordered entry stream and returns that digest in every `continuation_required` cursor together with `next_entry_index` and `next_entry_id`. Every resumed checkpoint must supply the original stream-digest cursor field; before current-state resolution, planning, receipt creation, or storage, the high-level CLI recomputes the digest over the supplied complete ordered stream and fails closed if it differs. Thus reordering, changing, truncating, or extending any entry—including one before or after the matching cursor pair—commits nothing. **Out of scope:** `jarvis sdd progress advance`, `AdvanceRequest`, storage, receipts, snapshot/evidence schemas, daemon, HTTP, MCP, Hive-client wire contracts, lifecycle, archive, legacy, and general sync remain unchanged. **Verification:** focused RED/GREEN tests followed by `cd hivederive && go test ./... && go vet ./...`, `cd jarvis-cli && go test ./... && go vet ./...`, `cd hive-daemon && go test ./... && go vet ./...`, `cd hive-api && go test ./... && go vet ./...`; preserve the full verification report/mirror protocol. **Rollback:** remove only the high-level checkpoint stream-digest validation and its canonical guidance; retain every committed snapshot, batch, receipt, and the low-level compatibility boundaries.

- [x] 6E.1 **RED:** In `hivederive/applyprogress/checkpoint_test.go`, add a deterministic canonical SHA-256 whole-stream digest fixture and failing resumed-checkpoint cases that start from one returned continuation cursor then reorder, modify, truncate, or extend entries while retaining the original matching `next_entry_index`, `next_entry_id`, expected state, and stream digest; assert each fails before a plan. In `jarvis-cli/cmd/jarvis/cmd_sdd_progress_test.go`, assert each malformed resumed request performs zero resolve/advance/storage calls and that first-checkpoint continuation output includes `stream_sha256` with the retained index/ID. <!-- sdd-owner: implementation -->
- [x] 6E.2 **GREEN:** In `hivederive/applyprogress/checkpoint.go`, add the deterministic digest of the complete ordered `EvidenceEntry` stream to the high-level checkpoint cursor/result contract, computing it from canonical typed entry serialization at the initial cursor and requiring the supplied prior digest to equal a fresh computation on every resume. In `jarvis-cli/cmd/jarvis/cmd_sdd_progress.go`, carry only this cursor binding through canonical checkpoint request/output validation before planner delegation; preserve `next_entry_index` and `next_entry_id`, and do not alter low-level `advance`, `AdvanceRequest`, HTTP, MCP, Hive client, or persistence contracts. <!-- sdd-owner: implementation -->
- [x] 6E.3 **TRIANGULATE/REFACTOR:** Extend focused planner and command tests for digest determinism across process-reproducible identical streams, Unicode entries, retry of the unchanged original request, and all four changed-stream classes with the cursor pair unchanged; confirm every mismatch returns the typed validation failure without planning, receipt, batch, snapshot, generation, coverage, or backend calls. Keep the complete PR 6E delta at or below 400 additions plus deletions. <!-- sdd-owner: implementation -->
- [x] 6E.4 **GREEN:** Update only `jarvis-cli/embed/skills/sdd-apply/SKILL.md`, `jarvis-cli/embed/orchestrator/sdd-orchestrator.md`, and `jarvis-cli/internal/skills/catalog_contract_test.go` so normal continuation guidance requires preserving and supplying `stream_sha256` unchanged with `next_entry_index` and `next_entry_id`; retain documented low-level `advance`/HTTP/MCP compatibility and never edit generated user-machine artifacts. <!-- sdd-owner: implementation -->
- [x] 6E.5 **TRIANGULATE/REFACTOR:** Rerun the full final scenario matrix: exact 162,725-rune repeated checkpoints across OpenSpec, Hive, and hybrid; unchanged same-request retry; changed/reordered/truncated/extended stream rejection before planning/storage even when index/entry ID match; all four outcomes; and unchanged low-level `advance`/HTTP/MCP behavior. Run the stated full module tests and vet, record commands/results in `openspec/changes/issue-653-bounded-apply-progress/verify-report.md`, and synchronize the OpenSpec `apply-progress.md` plus required Engram task/apply-progress mirrors without changing spec/design or generated artifacts. <!-- sdd-owner: implementation -->

## Parent Actions After Implementation

- [x] Historical delivery decision: the human selected `stacked-to-main`, authorized the PR 1 split into PR 1A/1B, and authorized the recorded `size:exception` boundaries for PR 1A (850), PR 1B (500), PR 2 (1,150 coherent CAS), PR 3 (500 audit limit), and PR 4A1 (550); PR 4A2, PR 4B1, PR 4B2A, PR 4B2B, and PR 5 remained under 400, while the 503-line combined PR 4B2 was superseded. <!-- sdd-owner: parent -->
- [ ] Start or reuse bounded review after each selected work unit; confirm tests, rollback boundary, and total diff against the 400-line budget. <!-- sdd-owner: parent -->
