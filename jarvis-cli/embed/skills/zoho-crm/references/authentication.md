# Authentication and deployment

Prefer a named OAuth connection for each supported authenticated operation. The CRM default is `conpas_crm`; use exact operation scopes rather than broadening scopes for convenience.

Configure OAuth clients, regional callback, sharing, target environment, and authorization through secure deployment configuration. Never request or embed access tokens, refresh tokens, client secrets, API keys, passwords, or credential-bearing URLs.
