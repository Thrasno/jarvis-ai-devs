# OS
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
logs/

# Temporary files
*.tmp
*.temp
.cache/

# Build artifacts
dist/
build/
*.exe
*.bin

# Local config (if any)
.env
.env.local
config.local.*

# Node modules (if we add any tooling later)
node_modules/

# PHP vendor (if we go PHP route)
vendor/

# Go build artifacts (if we go Go route)
*.test
*.out

# Go compiled binaries (without extension)
jarvis-cli/jarvis
hive-daemon/hive-daemon
hive-api/hive-api

# Claude Code worktrees
.claude/

# Local memory exports
HiveMemories/
memories.json

# Archives (extracted content should be tracked, not zips)
*.zip
*.tar.gz
*.rar
